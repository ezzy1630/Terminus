# pnpm workspace manifest (SPEC §42.1, §43.2).
# Terminus owns every workspace package. Bun remains the pinned development and
# test runner; no workspace entry is an inherited runtime compatibility layer.

packages:
  - "packages/*"
  - "apps/*"
  - "mini-services/terminus-control"
