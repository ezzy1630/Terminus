# @terminus/provider-conformance

Provider rendering exactness golden tests and exit gate per SPEC §38.19.

Tests exact message ordering, tool schemas, tool-choice semantics,
structured output, cache controls, continuation IDs, reasoning metadata,
token accounting, error projection, and confidentiality enforcement.
