// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: terminus/kernel/v1/kernel.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { Duration } from "../../../google/protobuf/duration";
import { Empty } from "../../../google/protobuf/empty";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "terminus.kernel.v1";

export enum PatchCommitMode {
  PATCH_COMMIT_MODE_UNSPECIFIED = 0,
  /** PATCH_COMMIT_MODE_PREVIEW_ONLY - Validate, write nothing */
  PATCH_COMMIT_MODE_PREVIEW_ONLY = 1,
  /** PATCH_COMMIT_MODE_STAGE_ONLY - Stage to journal */
  PATCH_COMMIT_MODE_STAGE_ONLY = 2,
  /** PATCH_COMMIT_MODE_APPLY_TO_WORKTREE - Commit to working tree */
  PATCH_COMMIT_MODE_APPLY_TO_WORKTREE = 3,
  UNRECOGNIZED = -1,
}

export enum EnforcementStatusProto {
  ENFORCEMENT_STATUS_UNSPECIFIED = 0,
  ENFORCED_STATUS = 1,
  DEGRADED_STATUS = 2,
  UNSUPPORTED_STATUS = 3,
  UNRECOGNIZED = -1,
}

export enum DecisionProto {
  DECISION_UNSPECIFIED = 0,
  DECISION_ALLOW = 1,
  DECISION_ALLOW_WITH_CONSTRAINTS = 2,
  DECISION_PROMPT = 3,
  DECISION_DENY = 4,
  UNRECOGNIZED = -1,
}

export enum CapabilityOperationProto {
  CAPABILITY_OPERATION_UNSPECIFIED = 0,
  CAPABILITY_OPERATION_READ = 1,
  CAPABILITY_OPERATION_PATCH = 2,
  CAPABILITY_OPERATION_EXEC = 3,
  CAPABILITY_OPERATION_JOB = 4,
  CAPABILITY_OPERATION_SANDBOX = 5,
  CAPABILITY_OPERATION_SECRET = 6,
  CAPABILITY_OPERATION_NETWORK = 7,
  CAPABILITY_OPERATION_CODE_INTEL = 8,
  CAPABILITY_OPERATION_EXTENSION = 9,
  CAPABILITY_OPERATION_GIT = 10,
  CAPABILITY_OPERATION_ARTIFACT_INGEST = 11,
  UNRECOGNIZED = -1,
}

/**
 * RequestContext is carried by every privileged kernel RPC. The capability
 * token MUST be validated against the kernel's instance identity before any
 * effect is authorized (SPEC §31.6).
 */
export interface RequestContext {
  /** UUIDv7 */
  requestId: string;
  /** Stable client-generated key for retries */
  idempotencyKey: string;
  sessionId: string;
  taskId: string;
  turnId: string;
  /** Principal driving the request */
  actorId: string;
  /** W3C Trace Context */
  traceparent: string;
  /** Short-lived kernel-issued capability */
  capabilityToken: string;
  workspaceId: string;
  deadline: Date | undefined;
  resourceBudgets: ResourceBudgets | undefined;
  policyVersion: string;
}

export interface ResourceBudgets {
  maxCpuMilliseconds: number;
  maxMemoryBytes: number;
  maxOutputBytes: number;
  maxWallclockSeconds: number;
}

/**
 * ArtifactRef references an immutable blob in the content-addressed store
 * (SPEC §29.3). `sha256` is the canonical `sha256:<hex>` form.
 */
export interface ArtifactRef {
  sha256: string;
  sizeBytes: number;
  mediaType: string;
}

/**
 * EffectIntent binds the request to the user's intent and the policy decision.
 * Taint sources propagate from untrusted content (Z5) into downstream policy
 * (SPEC §27.3, §36.15).
 */
export interface EffectIntent {
  /** URI of task contract / approval */
  userIntentRef: string;
  /** sha256 of the active contract */
  taskContractHash: string;
  /** trusted | derived | untrusted */
  trustLabel: string;
  /** public | workspace | secret_adjacent | secret */
  confidentialityLabel: string;
  /** URIs of untrusted provenance */
  taintSources: string[];
  /** e.g. secure-local-default */
  policyProfileId: string;
  /** SPEC §27.3 taxonomy */
  expectedEffectClass: string;
}

export interface WorkspacePath {
  workspaceId: string;
  /** Canonicalized within the workspace root */
  relativePath: string;
}

export interface SourceVersion {
  path: WorkspacePath | undefined;
  sha256: string;
  /** Git commit SHA or "no-vcs" */
  repositoryRevision: string;
}

export interface LineRange {
  startLine: number;
  endLine: number;
}

export interface Elision {
  range: LineRange | undefined;
  reason: string;
}

export interface Diagnostic {
  path: WorkspacePath | undefined;
  startLine: number;
  startColumn: number;
  endLine: number;
  endColumn: number;
  /** error | warning | info | hint */
  severity: string;
  /** e.g. "tsc", "rustc", "pyright" */
  source: string;
  code: string;
  message: string;
}

export interface PolicyDecision {
  decisionId: string;
  /** allow | deny | prompt */
  decision: string;
  ruleIds: string[];
  /** Safe, non-secret explanation */
  explanation: string;
}

export interface ReadFileRequest {
  context: RequestContext | undefined;
  intent: EffectIntent | undefined;
  path:
    | WorkspacePath
    | undefined;
  /** full | outline | ranges | symbols */
  mode: string;
  ranges: LineRange[];
  symbols: string[];
  /** Hard cap; truncation MUST be reported */
  maxBytes: number;
  /** Stale-write protection */
  expectedSha256: string;
}

export interface ReadFileResponse {
  sourceVersion:
    | SourceVersion
    | undefined;
  /** The mode actually used */
  renderedMode: string;
  /** Always available, may be elided in projection */
  fullContent:
    | ArtifactRef
    | undefined;
  /** What the model actually sees */
  modelProjectionUtf8: Uint8Array;
  elisions: Elision[];
  diagnostics: Diagnostic[];
  /** MUST be true if max_bytes hit */
  truncated: boolean;
  /** Opaque cursor for next page */
  continuationToken: string;
}

export interface WorkspaceBaseline {
  workspaceId: string;
  repositoryRevision: string;
  /** sha256 of the dirty working set */
  dirtyDigest: string;
  /** Per-file hashes (anchor set) */
  sources: SourceVersion[];
}

export interface PatchRequest {
  context: RequestContext | undefined;
  intent:
    | EffectIntent
    | undefined;
  /** Idempotency / journal key */
  transactionId: string;
  /** Snapshot anchor */
  baseline: WorkspaceBaseline | undefined;
  edits: PatchEdit[];
  /** e.g. "format-and-parse" */
  validationProfileId: string;
  allowTransientInvalidState: boolean;
  commitMode: PatchCommitMode;
}

export interface PatchEdit {
  replaceSymbol?: ReplaceSymbol | undefined;
  replaceRange?: ReplaceRange | undefined;
  replaceExactText?: ReplaceExactText | undefined;
  insert?: InsertContent | undefined;
  deleteRange?: DeleteRange | undefined;
  createFile?: CreateFile | undefined;
  moveFile?: MoveFile | undefined;
  deleteFile?: DeleteFile | undefined;
  unifiedDiff?: UnifiedDiff | undefined;
}

export interface ReplaceSymbol {
  path: WorkspacePath | undefined;
  expectedSha256: string;
  symbol: string;
  /** Tree-sitter AST fingerprint */
  structuralFingerprint: string;
  replacementUtf8: Uint8Array;
}

export interface ReplaceRange {
  path: WorkspacePath | undefined;
  expectedSha256: string;
  range: LineRange | undefined;
  replacementUtf8: Uint8Array;
}

export interface ReplaceExactText {
  path: WorkspacePath | undefined;
  expectedSha256: string;
  expectedUtf8: Uint8Array;
  replacementUtf8: Uint8Array;
  /** Reject if expected text appears >1 time */
  requireUnique: boolean;
}

export interface InsertContent {
  path: WorkspacePath | undefined;
  expectedSha256: string;
  /** symbol | line | range-start | range-end | eof */
  anchorKind: string;
  anchor: string;
  /** before | after | replace */
  position: string;
  contentUtf8: Uint8Array;
}

export interface DeleteRange {
  path: WorkspacePath | undefined;
  expectedSha256: string;
  range: LineRange | undefined;
}

export interface CreateFile {
  path:
    | WorkspacePath
    | undefined;
  /** Blind-create guard */
  mustNotExist: boolean;
  content: Uint8Array;
  mediaType: string;
}

export interface MoveFile {
  from: WorkspacePath | undefined;
  to: WorkspacePath | undefined;
  expectedSha256: string;
  targetMustNotExist: boolean;
}

export interface DeleteFile {
  path: WorkspacePath | undefined;
  expectedSha256: string;
}

export interface UnifiedDiff {
  repositoryRevision: string;
  diffUtf8: Uint8Array;
}

export interface ChangedFile {
  path: WorkspacePath | undefined;
  oldSha256: string;
  newSha256: string;
  /** create | modify | move | delete */
  operation: string;
}

export interface ValidationResult {
  /** e.g. "rustfmt", "tsc", "parse" */
  checkId: string;
  /** pass | fail | skipped */
  status: string;
  summary: string;
  /** Diagnostic output artifact */
  evidence: ArtifactRef | undefined;
}

export interface PatchResponse {
  transactionId: string;
  /** staged | applied | rolled-back | failed */
  state: string;
  finalRepositoryRevision: string;
  finalDirtyDigest: string;
  changedFiles: ChangedFile[];
  validations: ValidationResult[];
  /** Full unified diff artifact */
  completeDiff: ArtifactRef | undefined;
}

export interface PatchReconcileRequest {
  context:
    | RequestContext
    | undefined;
  /** Unknown-settlement reconciliation */
  transactionId: string;
}

export interface ShellSpec {
  enabled: boolean;
  /** bash | sh | powershell | cmd */
  dialect: string;
  /** Normalized script (NOT raw user input) */
  script: string;
}

export interface CommandSpec {
  program: string;
  args: string[];
  cwd:
    | WorkspacePath
    | undefined;
  /** Non-secret env only */
  publicEnv: { [key: string]: string };
  /** Broker handles injection */
  secretCapabilityUris: string[];
  timeout: Duration | undefined;
  allocatePty: boolean;
  shell: ShellSpec | undefined;
}

export interface CommandSpec_PublicEnvEntry {
  key: string;
  value: string;
}

export interface StartProcessRequest {
  context: RequestContext | undefined;
  intent: EffectIntent | undefined;
  command:
    | CommandSpec
    | undefined;
  /** e.g. "secure-local-default" */
  sandboxProfileId: string;
  /** Redaction / truncation policy */
  outputPolicyId: string;
}

export interface ProcessStarted {
  processId: string;
  jobId: string;
  /** Post-canonicalization */
  resolvedExecutable: string;
  startedAt: Date | undefined;
}

export interface OutputChunk {
  /** Monotonic per stream */
  cursor: number;
  bytes: Uint8Array;
  /** True if secrets were stripped */
  redacted: boolean;
}

export interface ProcessExited {
  exitCode: number;
  /** Empty if normal exit */
  signal: string;
  exitedAt:
    | Date
    | undefined;
  /** Full output spilled to CAS */
  stdoutArtifact: ArtifactRef | undefined;
  stderrArtifact: ArtifactRef | undefined;
}

export interface ProcessEvent {
  /** Monotonic per process */
  sequence: number;
  occurredAt: Date | undefined;
  started?: ProcessStarted | undefined;
  stdout?: OutputChunk | undefined;
  stderr?: OutputChunk | undefined;
  exited?:
    | ProcessExited
    | undefined;
  /** Pre-exec policy decision */
  policy?: PolicyDecision | undefined;
}

export interface CancelProcessRequest {
  context: RequestContext | undefined;
  processId: string;
  /** e.g. "timeout", "user-cancel", "budget-exceeded" */
  reason: string;
}

export interface CancelProcessResponse {
  /** cancelled | already-exited | unknown */
  state: string;
}

export interface StartJobRequest {
  context: RequestContext | undefined;
  intent: EffectIntent | undefined;
  command: CommandSpec | undefined;
  sandboxProfileId: string;
  outputPolicyId: string;
  /** Survives control-plane restart */
  durable: boolean;
}

export interface StartJobResponse {
  jobId: string;
  processId: string;
  startedAt: Date | undefined;
}

export interface JobStreamRequest {
  context: RequestContext | undefined;
  jobId: string;
  /** Resume cursor (SSE-style) */
  fromSequence: number;
}

export interface JobEvent {
  sequence: number;
  occurredAt: Date | undefined;
  started?: ProcessStarted | undefined;
  stdout?: OutputChunk | undefined;
  stderr?: OutputChunk | undefined;
  exited?: ProcessExited | undefined;
  policy?:
    | PolicyDecision
    | undefined;
  /** Emitted on restart recovery */
  reconciled?: JobReconciled | undefined;
}

export interface JobReconciled {
  /** running | exited | unknown-settlement */
  state: string;
  explanation: string;
}

export interface JobInputRequest {
  context: RequestContext | undefined;
  jobId: string;
  stdin: Uint8Array;
}

export interface JobSignalRequest {
  context: RequestContext | undefined;
  jobId: string;
  /** SIGTERM, SIGKILL, etc. */
  signal: string;
}

export interface JobStopRequest {
  context: RequestContext | undefined;
  jobId: string;
  reason: string;
}

export interface JobGetRequest {
  context: RequestContext | undefined;
  jobId: string;
}

export interface JobState {
  jobId: string;
  /** queued | running | exited | unknown */
  state: string;
  exitCode: number;
  startedAt: Date | undefined;
  exitedAt: Date | undefined;
  stdoutArtifact: ArtifactRef | undefined;
  stderrArtifact: ArtifactRef | undefined;
}

export interface KernelInfo {
  version: string;
  /** e.g. "terminus.kernel.v1" */
  protocolVersion: string;
  buildRevision: string;
  /** linux-bwrap | macos-sandbox | windows | container | microvm */
  supportedBackends: string[];
  supportedServices: string[];
  /**
   * Stable kernel identity for capability-token audience + mTLS SAN binding
   * (SPEC §48.14). Format: `kernel:<opaque>`.
   */
  instanceId: string;
}

export interface KernelHealth {
  /** healthy | degraded | failing */
  state: string;
  /** Empty if healthy */
  degradations: string[];
  checkedAt: Date | undefined;
}

export interface BootstrapControlRequest {
  /** Must equal the configured control principal */
  principal: string;
}

export interface BootstrapControlCapabilities {
  /** Admin, bound to task control-broker */
  brokerCapabilityToken: string;
  /** Admin, bound to task control-maintenance */
  maintenanceCapabilityToken: string;
  expiresAtUnix: number;
}

export interface RegisterWorkspaceRequest {
  context: RequestContext | undefined;
  rootUri: string;
  canonicalRoot: string;
  /** trusted | untrusted | restricted */
  trust: string;
  /** Optional remote environment descriptor JSON (SPEC §48.14). Empty for local. */
  remoteEnvironmentJson: string;
  /** workspace | container | microvm | remote */
  kind: string;
  /**
   * Existing control-plane identity to preserve during registry migration.
   * Empty for a new workspace. Admin authorization is required by the RPC.
   */
  requestedWorkspaceId: string;
}

export interface WorkspaceEntryMessage {
  id: string;
  rootUri: string;
  canonicalRoot: string;
  trust: string;
}

export interface ResolveWorkspaceRootRequest {
  context: RequestContext | undefined;
  rootUri: string;
  candidateRoot: string;
}

export interface ResolvedWorkspaceRootMessage {
  rootUri: string;
  canonicalRoot: string;
}

export interface GetWorkspaceRequest {
  context: RequestContext | undefined;
  workspaceId: string;
}

export interface EnforcementReportMessage {
  backendId: string;
  status: EnforcementStatusProto;
  enforced: string[];
  degraded: string[];
  unsupported: string[];
  notes: string[];
}

export interface SandboxReportRequest {
  context: RequestContext | undefined;
  profileId: string;
}

export interface NormalizedCommandMessage {
  resolvedExecutable: string;
  argv: string[];
  workingDirectory: string;
  effectTypes: string[];
  taintSources: string[];
}

export interface ConstraintMessage {
  maxRuntimeMs?: number | undefined;
  maxOutputBytes?: number | undefined;
  disallowedEnv: string[];
}

export interface DecisionReportMessage {
  decision: DecisionProto;
  ruleIds: string[];
  explanation: string;
  decisionId: string;
  constraints: ConstraintMessage | undefined;
}

export interface EvaluatePolicyRequest {
  context: RequestContext | undefined;
  command: NormalizedCommandMessage | undefined;
}

export interface MintTaskCapabilityRequest {
  /** Admin broker capability */
  context: RequestContext | undefined;
  principal: string;
  sessionId: string;
  taskId: string;
  workspaceId: string;
  operationClasses: CapabilityOperationProto[];
  workspacePaths: string[];
  networkDestinations: string[];
  secretCapabilities: string[];
  /** 1..300 */
  ttlSeconds: number;
}

export interface MintTaskCapabilityResponse {
  capabilityToken: string;
  expiresAtUnix: number;
}

export interface MintSecretRequest {
  context:
    | RequestContext
    | undefined;
  /** e.g. secret://providers/openai/api_key */
  capabilityUri: string;
  ttlSeconds: number;
}

export interface SecretCapabilityMessage {
  capabilityUri: string;
  /** opaque, model-visible handle (never the value) */
  handle: string;
  expiresAtUnix: number;
}

export interface StoreSecretRequest {
  context: RequestContext | undefined;
  capabilityUri: string;
  /** Maximum 16 KiB; never logged or persisted outside OS keyring */
  value: Uint8Array;
}

export interface DeleteSecretRequest {
  context: RequestContext | undefined;
  capabilityUri: string;
}

export interface SecretMutationResponse {
  capabilityUri: string;
  stored: boolean;
}

export interface EgressRequest {
  context:
    | RequestContext
    | undefined;
  /** host:port */
  destination: string;
  method: string;
}

export interface EgressDecisionMessage {
  allowed: boolean;
  reason: string;
}

export interface ConnectorGrantBindingMessage {
  connectorId: string;
  destinationHost: string;
  destinationPort: number;
  scheme: string;
  method: string;
  pathClass: string;
  effectId: string;
}

export interface MintConnectorGrantRequest {
  context: RequestContext | undefined;
  capabilityUri: string;
  binding: ConnectorGrantBindingMessage | undefined;
  ttlSeconds: number;
}

export interface ConnectorGrantMessage {
  encodedGrant: string;
  grantId: string;
  expiresAtUnix: number;
}

export interface ConnectorHeaderMessage {
  name: string;
  value: string;
}

export interface ConnectorOperationMessage {
  method: string;
  scheme: string;
  host: string;
  port: number;
  path: string;
  query: string;
  headers: ConnectorHeaderMessage[];
  body: Uint8Array;
}

export interface ExecuteConnectorRequest {
  context: RequestContext | undefined;
  encodedGrant: string;
  operation: ConnectorOperationMessage | undefined;
}

export interface ConnectorReceiptMessage {
  grantId: string;
  taskId: string;
  effectId: string;
  connectorId: string;
  method: string;
  path: string;
  destination: string;
  requestSha256: string;
  statusCode?: number | undefined;
  responseSha256?: string | undefined;
  responseRedactions: number;
  outcome: string;
}

export interface ConnectorResponseMessage {
  receipt: ConnectorReceiptMessage | undefined;
  body: Uint8Array;
  contentType?: string | undefined;
}

export interface CodeSearchRequest {
  context: RequestContext | undefined;
  workspaceId: string;
  query: string;
  limit: number;
}

export interface CodeSearchResult {
  path: string;
  line: number;
  symbol: string;
  /** lexical_bm25 | tree_sitter | lsp | graph */
  method: string;
}

export interface CodeSearchResponse {
  results: CodeSearchResult[];
  truncated: boolean;
  continuation?: string | undefined;
}

export interface ExtensionInvokeRequest {
  context: RequestContext | undefined;
  capabilityId: string;
  operation: string;
  input: Uint8Array;
}

export interface ExtensionInvokeResponse {
  output: Uint8Array;
  ok: boolean;
  error: string;
}

export interface IngestArtifactRequest {
  context: RequestContext | undefined;
  content: Uint8Array;
  mediaType: string;
}

export interface IngestArtifactResponse {
  artifact: ArtifactRef | undefined;
  alreadyPresent: boolean;
}

export interface GetArtifactRequest {
  context: RequestContext | undefined;
  sha256: string;
}

export interface GetArtifactResponse {
  artifact: ArtifactRef | undefined;
  content: Uint8Array;
}

export interface GetArtifactMetadataRequest {
  context: RequestContext | undefined;
  sha256: string;
}

export interface GetArtifactMetadataResponse {
  artifact: ArtifactRef | undefined;
}

export interface LinkArtifactRequest {
  context: RequestContext | undefined;
  sha256: string;
  ownerType: string;
  ownerId: string;
  purpose: string;
  /**
   * Authoritative task aggregate that owns the admission. The kernel binds
   * this to both RequestContext.task_id and the capability token task binder.
   */
  ownerTaskId: string;
}

export interface LinkArtifactResponse {
  linked: boolean;
}

export interface ListCheckpointArtifactLinksRequest {
  context: RequestContext | undefined;
  pageSize: number;
  continuationToken: string;
}

export interface CheckpointArtifactLinkMessage {
  linkId: string;
  sha256: string;
  checkpointId: string;
  ownerTaskId: string;
  createdAt: string;
}

export interface ListCheckpointArtifactLinksResponse {
  links: CheckpointArtifactLinkMessage[];
  continuationToken: string;
}

export interface UnlinkCheckpointArtifactRequest {
  context: RequestContext | undefined;
  sha256: string;
  checkpointId: string;
  ownerTaskId: string;
}

export interface UnlinkCheckpointArtifactResponse {
  unlinked: boolean;
}

function createBaseRequestContext(): RequestContext {
  return {
    requestId: "",
    idempotencyKey: "",
    sessionId: "",
    taskId: "",
    turnId: "",
    actorId: "",
    traceparent: "",
    capabilityToken: "",
    workspaceId: "",
    deadline: undefined,
    resourceBudgets: undefined,
    policyVersion: "",
  };
}

export const RequestContext: MessageFns<RequestContext> = {
  encode(message: RequestContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.idempotencyKey !== "") {
      writer.uint32(18).string(message.idempotencyKey);
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    if (message.taskId !== "") {
      writer.uint32(34).string(message.taskId);
    }
    if (message.turnId !== "") {
      writer.uint32(42).string(message.turnId);
    }
    if (message.actorId !== "") {
      writer.uint32(50).string(message.actorId);
    }
    if (message.traceparent !== "") {
      writer.uint32(58).string(message.traceparent);
    }
    if (message.capabilityToken !== "") {
      writer.uint32(66).string(message.capabilityToken);
    }
    if (message.workspaceId !== "") {
      writer.uint32(74).string(message.workspaceId);
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(82).fork()).join();
    }
    if (message.resourceBudgets !== undefined) {
      ResourceBudgets.encode(message.resourceBudgets, writer.uint32(90).fork()).join();
    }
    if (message.policyVersion !== "") {
      writer.uint32(98).string(message.policyVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.turnId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.actorId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.traceparent = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.capabilityToken = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.resourceBudgets = ResourceBudgets.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.policyVersion = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RequestContext>, I>>(base?: I): RequestContext {
    return RequestContext.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestContext>, I>>(object: I): RequestContext {
    const message = createBaseRequestContext();
    message.requestId = object.requestId ?? "";
    message.idempotencyKey = object.idempotencyKey ?? "";
    message.sessionId = object.sessionId ?? "";
    message.taskId = object.taskId ?? "";
    message.turnId = object.turnId ?? "";
    message.actorId = object.actorId ?? "";
    message.traceparent = object.traceparent ?? "";
    message.capabilityToken = object.capabilityToken ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.deadline = object.deadline ?? undefined;
    message.resourceBudgets = (object.resourceBudgets !== undefined && object.resourceBudgets !== null)
      ? ResourceBudgets.fromPartial(object.resourceBudgets)
      : undefined;
    message.policyVersion = object.policyVersion ?? "";
    return message;
  },
};

function createBaseResourceBudgets(): ResourceBudgets {
  return { maxCpuMilliseconds: 0, maxMemoryBytes: 0, maxOutputBytes: 0, maxWallclockSeconds: 0 };
}

export const ResourceBudgets: MessageFns<ResourceBudgets> = {
  encode(message: ResourceBudgets, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxCpuMilliseconds !== 0) {
      writer.uint32(8).uint64(message.maxCpuMilliseconds);
    }
    if (message.maxMemoryBytes !== 0) {
      writer.uint32(16).uint64(message.maxMemoryBytes);
    }
    if (message.maxOutputBytes !== 0) {
      writer.uint32(24).uint64(message.maxOutputBytes);
    }
    if (message.maxWallclockSeconds !== 0) {
      writer.uint32(32).uint64(message.maxWallclockSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceBudgets {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceBudgets();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxCpuMilliseconds = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxMemoryBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxOutputBytes = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxWallclockSeconds = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResourceBudgets>, I>>(base?: I): ResourceBudgets {
    return ResourceBudgets.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceBudgets>, I>>(object: I): ResourceBudgets {
    const message = createBaseResourceBudgets();
    message.maxCpuMilliseconds = object.maxCpuMilliseconds ?? 0;
    message.maxMemoryBytes = object.maxMemoryBytes ?? 0;
    message.maxOutputBytes = object.maxOutputBytes ?? 0;
    message.maxWallclockSeconds = object.maxWallclockSeconds ?? 0;
    return message;
  },
};

function createBaseArtifactRef(): ArtifactRef {
  return { sha256: "", sizeBytes: 0, mediaType: "" };
}

export const ArtifactRef: MessageFns<ArtifactRef> = {
  encode(message: ArtifactRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sha256 !== "") {
      writer.uint32(10).string(message.sha256);
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(16).uint64(message.sizeBytes);
    }
    if (message.mediaType !== "") {
      writer.uint32(26).string(message.mediaType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sizeBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ArtifactRef>, I>>(base?: I): ArtifactRef {
    return ArtifactRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactRef>, I>>(object: I): ArtifactRef {
    const message = createBaseArtifactRef();
    message.sha256 = object.sha256 ?? "";
    message.sizeBytes = object.sizeBytes ?? 0;
    message.mediaType = object.mediaType ?? "";
    return message;
  },
};

function createBaseEffectIntent(): EffectIntent {
  return {
    userIntentRef: "",
    taskContractHash: "",
    trustLabel: "",
    confidentialityLabel: "",
    taintSources: [],
    policyProfileId: "",
    expectedEffectClass: "",
  };
}

export const EffectIntent: MessageFns<EffectIntent> = {
  encode(message: EffectIntent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userIntentRef !== "") {
      writer.uint32(10).string(message.userIntentRef);
    }
    if (message.taskContractHash !== "") {
      writer.uint32(18).string(message.taskContractHash);
    }
    if (message.trustLabel !== "") {
      writer.uint32(26).string(message.trustLabel);
    }
    if (message.confidentialityLabel !== "") {
      writer.uint32(34).string(message.confidentialityLabel);
    }
    for (const v of message.taintSources) {
      writer.uint32(42).string(v!);
    }
    if (message.policyProfileId !== "") {
      writer.uint32(50).string(message.policyProfileId);
    }
    if (message.expectedEffectClass !== "") {
      writer.uint32(58).string(message.expectedEffectClass);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectIntent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffectIntent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userIntentRef = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.taskContractHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.trustLabel = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.confidentialityLabel = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.taintSources.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.policyProfileId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.expectedEffectClass = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EffectIntent>, I>>(base?: I): EffectIntent {
    return EffectIntent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EffectIntent>, I>>(object: I): EffectIntent {
    const message = createBaseEffectIntent();
    message.userIntentRef = object.userIntentRef ?? "";
    message.taskContractHash = object.taskContractHash ?? "";
    message.trustLabel = object.trustLabel ?? "";
    message.confidentialityLabel = object.confidentialityLabel ?? "";
    message.taintSources = object.taintSources?.map((e) => e) || [];
    message.policyProfileId = object.policyProfileId ?? "";
    message.expectedEffectClass = object.expectedEffectClass ?? "";
    return message;
  },
};

function createBaseWorkspacePath(): WorkspacePath {
  return { workspaceId: "", relativePath: "" };
}

export const WorkspacePath: MessageFns<WorkspacePath> = {
  encode(message: WorkspacePath, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.relativePath !== "") {
      writer.uint32(18).string(message.relativePath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspacePath {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspacePath();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relativePath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkspacePath>, I>>(base?: I): WorkspacePath {
    return WorkspacePath.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkspacePath>, I>>(object: I): WorkspacePath {
    const message = createBaseWorkspacePath();
    message.workspaceId = object.workspaceId ?? "";
    message.relativePath = object.relativePath ?? "";
    return message;
  },
};

function createBaseSourceVersion(): SourceVersion {
  return { path: undefined, sha256: "", repositoryRevision: "" };
}

export const SourceVersion: MessageFns<SourceVersion> = {
  encode(message: SourceVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.sha256 !== "") {
      writer.uint32(18).string(message.sha256);
    }
    if (message.repositoryRevision !== "") {
      writer.uint32(26).string(message.repositoryRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SourceVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSourceVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.repositoryRevision = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SourceVersion>, I>>(base?: I): SourceVersion {
    return SourceVersion.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SourceVersion>, I>>(object: I): SourceVersion {
    const message = createBaseSourceVersion();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.sha256 = object.sha256 ?? "";
    message.repositoryRevision = object.repositoryRevision ?? "";
    return message;
  },
};

function createBaseLineRange(): LineRange {
  return { startLine: 0, endLine: 0 };
}

export const LineRange: MessageFns<LineRange> = {
  encode(message: LineRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startLine !== 0) {
      writer.uint32(8).uint32(message.startLine);
    }
    if (message.endLine !== 0) {
      writer.uint32(16).uint32(message.endLine);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LineRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLineRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.startLine = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.endLine = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<LineRange>, I>>(base?: I): LineRange {
    return LineRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LineRange>, I>>(object: I): LineRange {
    const message = createBaseLineRange();
    message.startLine = object.startLine ?? 0;
    message.endLine = object.endLine ?? 0;
    return message;
  },
};

function createBaseElision(): Elision {
  return { range: undefined, reason: "" };
}

export const Elision: MessageFns<Elision> = {
  encode(message: Elision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.range !== undefined) {
      LineRange.encode(message.range, writer.uint32(10).fork()).join();
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Elision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseElision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.range = LineRange.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Elision>, I>>(base?: I): Elision {
    return Elision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Elision>, I>>(object: I): Elision {
    const message = createBaseElision();
    message.range = (object.range !== undefined && object.range !== null)
      ? LineRange.fromPartial(object.range)
      : undefined;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseDiagnostic(): Diagnostic {
  return {
    path: undefined,
    startLine: 0,
    startColumn: 0,
    endLine: 0,
    endColumn: 0,
    severity: "",
    source: "",
    code: "",
    message: "",
  };
}

export const Diagnostic: MessageFns<Diagnostic> = {
  encode(message: Diagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.startLine !== 0) {
      writer.uint32(16).uint32(message.startLine);
    }
    if (message.startColumn !== 0) {
      writer.uint32(24).uint32(message.startColumn);
    }
    if (message.endLine !== 0) {
      writer.uint32(32).uint32(message.endLine);
    }
    if (message.endColumn !== 0) {
      writer.uint32(40).uint32(message.endColumn);
    }
    if (message.severity !== "") {
      writer.uint32(50).string(message.severity);
    }
    if (message.source !== "") {
      writer.uint32(58).string(message.source);
    }
    if (message.code !== "") {
      writer.uint32(66).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(74).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Diagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.startLine = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.startColumn = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.endLine = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.endColumn = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.severity = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Diagnostic>, I>>(base?: I): Diagnostic {
    return Diagnostic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Diagnostic>, I>>(object: I): Diagnostic {
    const message = createBaseDiagnostic();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.startLine = object.startLine ?? 0;
    message.startColumn = object.startColumn ?? 0;
    message.endLine = object.endLine ?? 0;
    message.endColumn = object.endColumn ?? 0;
    message.severity = object.severity ?? "";
    message.source = object.source ?? "";
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBasePolicyDecision(): PolicyDecision {
  return { decisionId: "", decision: "", ruleIds: [], explanation: "" };
}

export const PolicyDecision: MessageFns<PolicyDecision> = {
  encode(message: PolicyDecision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.decisionId !== "") {
      writer.uint32(10).string(message.decisionId);
    }
    if (message.decision !== "") {
      writer.uint32(18).string(message.decision);
    }
    for (const v of message.ruleIds) {
      writer.uint32(26).string(v!);
    }
    if (message.explanation !== "") {
      writer.uint32(34).string(message.explanation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyDecision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyDecision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.decisionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.decision = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ruleIds.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.explanation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PolicyDecision>, I>>(base?: I): PolicyDecision {
    return PolicyDecision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyDecision>, I>>(object: I): PolicyDecision {
    const message = createBasePolicyDecision();
    message.decisionId = object.decisionId ?? "";
    message.decision = object.decision ?? "";
    message.ruleIds = object.ruleIds?.map((e) => e) || [];
    message.explanation = object.explanation ?? "";
    return message;
  },
};

function createBaseReadFileRequest(): ReadFileRequest {
  return {
    context: undefined,
    intent: undefined,
    path: undefined,
    mode: "",
    ranges: [],
    symbols: [],
    maxBytes: 0,
    expectedSha256: "",
  };
}

export const ReadFileRequest: MessageFns<ReadFileRequest> = {
  encode(message: ReadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.intent !== undefined) {
      EffectIntent.encode(message.intent, writer.uint32(18).fork()).join();
    }
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(26).fork()).join();
    }
    if (message.mode !== "") {
      writer.uint32(34).string(message.mode);
    }
    for (const v of message.ranges) {
      LineRange.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.symbols) {
      writer.uint32(50).string(v!);
    }
    if (message.maxBytes !== 0) {
      writer.uint32(56).uint64(message.maxBytes);
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(66).string(message.expectedSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.intent = EffectIntent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ranges.push(LineRange.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.symbols.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxBytes = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReadFileRequest>, I>>(base?: I): ReadFileRequest {
    return ReadFileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadFileRequest>, I>>(object: I): ReadFileRequest {
    const message = createBaseReadFileRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.intent = (object.intent !== undefined && object.intent !== null)
      ? EffectIntent.fromPartial(object.intent)
      : undefined;
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.mode = object.mode ?? "";
    message.ranges = object.ranges?.map((e) => LineRange.fromPartial(e)) || [];
    message.symbols = object.symbols?.map((e) => e) || [];
    message.maxBytes = object.maxBytes ?? 0;
    message.expectedSha256 = object.expectedSha256 ?? "";
    return message;
  },
};

function createBaseReadFileResponse(): ReadFileResponse {
  return {
    sourceVersion: undefined,
    renderedMode: "",
    fullContent: undefined,
    modelProjectionUtf8: new Uint8Array(0),
    elisions: [],
    diagnostics: [],
    truncated: false,
    continuationToken: "",
  };
}

export const ReadFileResponse: MessageFns<ReadFileResponse> = {
  encode(message: ReadFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceVersion !== undefined) {
      SourceVersion.encode(message.sourceVersion, writer.uint32(10).fork()).join();
    }
    if (message.renderedMode !== "") {
      writer.uint32(18).string(message.renderedMode);
    }
    if (message.fullContent !== undefined) {
      ArtifactRef.encode(message.fullContent, writer.uint32(26).fork()).join();
    }
    if (message.modelProjectionUtf8.length !== 0) {
      writer.uint32(34).bytes(message.modelProjectionUtf8);
    }
    for (const v of message.elisions) {
      Elision.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.diagnostics) {
      Diagnostic.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.truncated !== false) {
      writer.uint32(56).bool(message.truncated);
    }
    if (message.continuationToken !== "") {
      writer.uint32(66).string(message.continuationToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sourceVersion = SourceVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.renderedMode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fullContent = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modelProjectionUtf8 = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.elisions.push(Elision.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.diagnostics.push(Diagnostic.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.continuationToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReadFileResponse>, I>>(base?: I): ReadFileResponse {
    return ReadFileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadFileResponse>, I>>(object: I): ReadFileResponse {
    const message = createBaseReadFileResponse();
    message.sourceVersion = (object.sourceVersion !== undefined && object.sourceVersion !== null)
      ? SourceVersion.fromPartial(object.sourceVersion)
      : undefined;
    message.renderedMode = object.renderedMode ?? "";
    message.fullContent = (object.fullContent !== undefined && object.fullContent !== null)
      ? ArtifactRef.fromPartial(object.fullContent)
      : undefined;
    message.modelProjectionUtf8 = object.modelProjectionUtf8 ?? new Uint8Array(0);
    message.elisions = object.elisions?.map((e) => Elision.fromPartial(e)) || [];
    message.diagnostics = object.diagnostics?.map((e) => Diagnostic.fromPartial(e)) || [];
    message.truncated = object.truncated ?? false;
    message.continuationToken = object.continuationToken ?? "";
    return message;
  },
};

function createBaseWorkspaceBaseline(): WorkspaceBaseline {
  return { workspaceId: "", repositoryRevision: "", dirtyDigest: "", sources: [] };
}

export const WorkspaceBaseline: MessageFns<WorkspaceBaseline> = {
  encode(message: WorkspaceBaseline, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.repositoryRevision !== "") {
      writer.uint32(18).string(message.repositoryRevision);
    }
    if (message.dirtyDigest !== "") {
      writer.uint32(26).string(message.dirtyDigest);
    }
    for (const v of message.sources) {
      SourceVersion.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceBaseline {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceBaseline();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repositoryRevision = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dirtyDigest = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sources.push(SourceVersion.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkspaceBaseline>, I>>(base?: I): WorkspaceBaseline {
    return WorkspaceBaseline.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkspaceBaseline>, I>>(object: I): WorkspaceBaseline {
    const message = createBaseWorkspaceBaseline();
    message.workspaceId = object.workspaceId ?? "";
    message.repositoryRevision = object.repositoryRevision ?? "";
    message.dirtyDigest = object.dirtyDigest ?? "";
    message.sources = object.sources?.map((e) => SourceVersion.fromPartial(e)) || [];
    return message;
  },
};

function createBasePatchRequest(): PatchRequest {
  return {
    context: undefined,
    intent: undefined,
    transactionId: "",
    baseline: undefined,
    edits: [],
    validationProfileId: "",
    allowTransientInvalidState: false,
    commitMode: 0,
  };
}

export const PatchRequest: MessageFns<PatchRequest> = {
  encode(message: PatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.intent !== undefined) {
      EffectIntent.encode(message.intent, writer.uint32(18).fork()).join();
    }
    if (message.transactionId !== "") {
      writer.uint32(26).string(message.transactionId);
    }
    if (message.baseline !== undefined) {
      WorkspaceBaseline.encode(message.baseline, writer.uint32(34).fork()).join();
    }
    for (const v of message.edits) {
      PatchEdit.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.validationProfileId !== "") {
      writer.uint32(50).string(message.validationProfileId);
    }
    if (message.allowTransientInvalidState !== false) {
      writer.uint32(56).bool(message.allowTransientInvalidState);
    }
    if (message.commitMode !== 0) {
      writer.uint32(64).int32(message.commitMode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.intent = EffectIntent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.transactionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.baseline = WorkspaceBaseline.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.edits.push(PatchEdit.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.validationProfileId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.allowTransientInvalidState = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.commitMode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PatchRequest>, I>>(base?: I): PatchRequest {
    return PatchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PatchRequest>, I>>(object: I): PatchRequest {
    const message = createBasePatchRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.intent = (object.intent !== undefined && object.intent !== null)
      ? EffectIntent.fromPartial(object.intent)
      : undefined;
    message.transactionId = object.transactionId ?? "";
    message.baseline = (object.baseline !== undefined && object.baseline !== null)
      ? WorkspaceBaseline.fromPartial(object.baseline)
      : undefined;
    message.edits = object.edits?.map((e) => PatchEdit.fromPartial(e)) || [];
    message.validationProfileId = object.validationProfileId ?? "";
    message.allowTransientInvalidState = object.allowTransientInvalidState ?? false;
    message.commitMode = object.commitMode ?? 0;
    return message;
  },
};

function createBasePatchEdit(): PatchEdit {
  return {
    replaceSymbol: undefined,
    replaceRange: undefined,
    replaceExactText: undefined,
    insert: undefined,
    deleteRange: undefined,
    createFile: undefined,
    moveFile: undefined,
    deleteFile: undefined,
    unifiedDiff: undefined,
  };
}

export const PatchEdit: MessageFns<PatchEdit> = {
  encode(message: PatchEdit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.replaceSymbol !== undefined) {
      ReplaceSymbol.encode(message.replaceSymbol, writer.uint32(82).fork()).join();
    }
    if (message.replaceRange !== undefined) {
      ReplaceRange.encode(message.replaceRange, writer.uint32(90).fork()).join();
    }
    if (message.replaceExactText !== undefined) {
      ReplaceExactText.encode(message.replaceExactText, writer.uint32(98).fork()).join();
    }
    if (message.insert !== undefined) {
      InsertContent.encode(message.insert, writer.uint32(106).fork()).join();
    }
    if (message.deleteRange !== undefined) {
      DeleteRange.encode(message.deleteRange, writer.uint32(114).fork()).join();
    }
    if (message.createFile !== undefined) {
      CreateFile.encode(message.createFile, writer.uint32(122).fork()).join();
    }
    if (message.moveFile !== undefined) {
      MoveFile.encode(message.moveFile, writer.uint32(130).fork()).join();
    }
    if (message.deleteFile !== undefined) {
      DeleteFile.encode(message.deleteFile, writer.uint32(138).fork()).join();
    }
    if (message.unifiedDiff !== undefined) {
      UnifiedDiff.encode(message.unifiedDiff, writer.uint32(146).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchEdit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchEdit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.replaceSymbol = ReplaceSymbol.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.replaceRange = ReplaceRange.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.replaceExactText = ReplaceExactText.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.insert = InsertContent.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.deleteRange = DeleteRange.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.createFile = CreateFile.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.moveFile = MoveFile.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.deleteFile = DeleteFile.decode(reader, reader.uint32());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.unifiedDiff = UnifiedDiff.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PatchEdit>, I>>(base?: I): PatchEdit {
    return PatchEdit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PatchEdit>, I>>(object: I): PatchEdit {
    const message = createBasePatchEdit();
    message.replaceSymbol = (object.replaceSymbol !== undefined && object.replaceSymbol !== null)
      ? ReplaceSymbol.fromPartial(object.replaceSymbol)
      : undefined;
    message.replaceRange = (object.replaceRange !== undefined && object.replaceRange !== null)
      ? ReplaceRange.fromPartial(object.replaceRange)
      : undefined;
    message.replaceExactText = (object.replaceExactText !== undefined && object.replaceExactText !== null)
      ? ReplaceExactText.fromPartial(object.replaceExactText)
      : undefined;
    message.insert = (object.insert !== undefined && object.insert !== null)
      ? InsertContent.fromPartial(object.insert)
      : undefined;
    message.deleteRange = (object.deleteRange !== undefined && object.deleteRange !== null)
      ? DeleteRange.fromPartial(object.deleteRange)
      : undefined;
    message.createFile = (object.createFile !== undefined && object.createFile !== null)
      ? CreateFile.fromPartial(object.createFile)
      : undefined;
    message.moveFile = (object.moveFile !== undefined && object.moveFile !== null)
      ? MoveFile.fromPartial(object.moveFile)
      : undefined;
    message.deleteFile = (object.deleteFile !== undefined && object.deleteFile !== null)
      ? DeleteFile.fromPartial(object.deleteFile)
      : undefined;
    message.unifiedDiff = (object.unifiedDiff !== undefined && object.unifiedDiff !== null)
      ? UnifiedDiff.fromPartial(object.unifiedDiff)
      : undefined;
    return message;
  },
};

function createBaseReplaceSymbol(): ReplaceSymbol {
  return {
    path: undefined,
    expectedSha256: "",
    symbol: "",
    structuralFingerprint: "",
    replacementUtf8: new Uint8Array(0),
  };
}

export const ReplaceSymbol: MessageFns<ReplaceSymbol> = {
  encode(message: ReplaceSymbol, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(18).string(message.expectedSha256);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.structuralFingerprint !== "") {
      writer.uint32(34).string(message.structuralFingerprint);
    }
    if (message.replacementUtf8.length !== 0) {
      writer.uint32(42).bytes(message.replacementUtf8);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplaceSymbol {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplaceSymbol();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.structuralFingerprint = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.replacementUtf8 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReplaceSymbol>, I>>(base?: I): ReplaceSymbol {
    return ReplaceSymbol.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReplaceSymbol>, I>>(object: I): ReplaceSymbol {
    const message = createBaseReplaceSymbol();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    message.symbol = object.symbol ?? "";
    message.structuralFingerprint = object.structuralFingerprint ?? "";
    message.replacementUtf8 = object.replacementUtf8 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseReplaceRange(): ReplaceRange {
  return { path: undefined, expectedSha256: "", range: undefined, replacementUtf8: new Uint8Array(0) };
}

export const ReplaceRange: MessageFns<ReplaceRange> = {
  encode(message: ReplaceRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(18).string(message.expectedSha256);
    }
    if (message.range !== undefined) {
      LineRange.encode(message.range, writer.uint32(26).fork()).join();
    }
    if (message.replacementUtf8.length !== 0) {
      writer.uint32(34).bytes(message.replacementUtf8);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplaceRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplaceRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.range = LineRange.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.replacementUtf8 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReplaceRange>, I>>(base?: I): ReplaceRange {
    return ReplaceRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReplaceRange>, I>>(object: I): ReplaceRange {
    const message = createBaseReplaceRange();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    message.range = (object.range !== undefined && object.range !== null)
      ? LineRange.fromPartial(object.range)
      : undefined;
    message.replacementUtf8 = object.replacementUtf8 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseReplaceExactText(): ReplaceExactText {
  return {
    path: undefined,
    expectedSha256: "",
    expectedUtf8: new Uint8Array(0),
    replacementUtf8: new Uint8Array(0),
    requireUnique: false,
  };
}

export const ReplaceExactText: MessageFns<ReplaceExactText> = {
  encode(message: ReplaceExactText, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(18).string(message.expectedSha256);
    }
    if (message.expectedUtf8.length !== 0) {
      writer.uint32(26).bytes(message.expectedUtf8);
    }
    if (message.replacementUtf8.length !== 0) {
      writer.uint32(34).bytes(message.replacementUtf8);
    }
    if (message.requireUnique !== false) {
      writer.uint32(40).bool(message.requireUnique);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplaceExactText {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplaceExactText();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expectedUtf8 = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.replacementUtf8 = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.requireUnique = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReplaceExactText>, I>>(base?: I): ReplaceExactText {
    return ReplaceExactText.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReplaceExactText>, I>>(object: I): ReplaceExactText {
    const message = createBaseReplaceExactText();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    message.expectedUtf8 = object.expectedUtf8 ?? new Uint8Array(0);
    message.replacementUtf8 = object.replacementUtf8 ?? new Uint8Array(0);
    message.requireUnique = object.requireUnique ?? false;
    return message;
  },
};

function createBaseInsertContent(): InsertContent {
  return {
    path: undefined,
    expectedSha256: "",
    anchorKind: "",
    anchor: "",
    position: "",
    contentUtf8: new Uint8Array(0),
  };
}

export const InsertContent: MessageFns<InsertContent> = {
  encode(message: InsertContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(18).string(message.expectedSha256);
    }
    if (message.anchorKind !== "") {
      writer.uint32(26).string(message.anchorKind);
    }
    if (message.anchor !== "") {
      writer.uint32(34).string(message.anchor);
    }
    if (message.position !== "") {
      writer.uint32(42).string(message.position);
    }
    if (message.contentUtf8.length !== 0) {
      writer.uint32(50).bytes(message.contentUtf8);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InsertContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInsertContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.anchorKind = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.anchor = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.position = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.contentUtf8 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<InsertContent>, I>>(base?: I): InsertContent {
    return InsertContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InsertContent>, I>>(object: I): InsertContent {
    const message = createBaseInsertContent();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    message.anchorKind = object.anchorKind ?? "";
    message.anchor = object.anchor ?? "";
    message.position = object.position ?? "";
    message.contentUtf8 = object.contentUtf8 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseDeleteRange(): DeleteRange {
  return { path: undefined, expectedSha256: "", range: undefined };
}

export const DeleteRange: MessageFns<DeleteRange> = {
  encode(message: DeleteRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(18).string(message.expectedSha256);
    }
    if (message.range !== undefined) {
      LineRange.encode(message.range, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.range = LineRange.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<DeleteRange>, I>>(base?: I): DeleteRange {
    return DeleteRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRange>, I>>(object: I): DeleteRange {
    const message = createBaseDeleteRange();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    message.range = (object.range !== undefined && object.range !== null)
      ? LineRange.fromPartial(object.range)
      : undefined;
    return message;
  },
};

function createBaseCreateFile(): CreateFile {
  return { path: undefined, mustNotExist: false, content: new Uint8Array(0), mediaType: "" };
}

export const CreateFile: MessageFns<CreateFile> = {
  encode(message: CreateFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.mustNotExist !== false) {
      writer.uint32(16).bool(message.mustNotExist);
    }
    if (message.content.length !== 0) {
      writer.uint32(26).bytes(message.content);
    }
    if (message.mediaType !== "") {
      writer.uint32(34).string(message.mediaType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mustNotExist = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CreateFile>, I>>(base?: I): CreateFile {
    return CreateFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateFile>, I>>(object: I): CreateFile {
    const message = createBaseCreateFile();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.mustNotExist = object.mustNotExist ?? false;
    message.content = object.content ?? new Uint8Array(0);
    message.mediaType = object.mediaType ?? "";
    return message;
  },
};

function createBaseMoveFile(): MoveFile {
  return { from: undefined, to: undefined, expectedSha256: "", targetMustNotExist: false };
}

export const MoveFile: MessageFns<MoveFile> = {
  encode(message: MoveFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== undefined) {
      WorkspacePath.encode(message.from, writer.uint32(10).fork()).join();
    }
    if (message.to !== undefined) {
      WorkspacePath.encode(message.to, writer.uint32(18).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(26).string(message.expectedSha256);
    }
    if (message.targetMustNotExist !== false) {
      writer.uint32(32).bool(message.targetMustNotExist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.targetMustNotExist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<MoveFile>, I>>(base?: I): MoveFile {
    return MoveFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MoveFile>, I>>(object: I): MoveFile {
    const message = createBaseMoveFile();
    message.from = (object.from !== undefined && object.from !== null)
      ? WorkspacePath.fromPartial(object.from)
      : undefined;
    message.to = (object.to !== undefined && object.to !== null) ? WorkspacePath.fromPartial(object.to) : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    message.targetMustNotExist = object.targetMustNotExist ?? false;
    return message;
  },
};

function createBaseDeleteFile(): DeleteFile {
  return { path: undefined, expectedSha256: "" };
}

export const DeleteFile: MessageFns<DeleteFile> = {
  encode(message: DeleteFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.expectedSha256 !== "") {
      writer.uint32(18).string(message.expectedSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedSha256 = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<DeleteFile>, I>>(base?: I): DeleteFile {
    return DeleteFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteFile>, I>>(object: I): DeleteFile {
    const message = createBaseDeleteFile();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.expectedSha256 = object.expectedSha256 ?? "";
    return message;
  },
};

function createBaseUnifiedDiff(): UnifiedDiff {
  return { repositoryRevision: "", diffUtf8: new Uint8Array(0) };
}

export const UnifiedDiff: MessageFns<UnifiedDiff> = {
  encode(message: UnifiedDiff, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repositoryRevision !== "") {
      writer.uint32(10).string(message.repositoryRevision);
    }
    if (message.diffUtf8.length !== 0) {
      writer.uint32(18).bytes(message.diffUtf8);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnifiedDiff {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnifiedDiff();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repositoryRevision = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.diffUtf8 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<UnifiedDiff>, I>>(base?: I): UnifiedDiff {
    return UnifiedDiff.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnifiedDiff>, I>>(object: I): UnifiedDiff {
    const message = createBaseUnifiedDiff();
    message.repositoryRevision = object.repositoryRevision ?? "";
    message.diffUtf8 = object.diffUtf8 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseChangedFile(): ChangedFile {
  return { path: undefined, oldSha256: "", newSha256: "", operation: "" };
}

export const ChangedFile: MessageFns<ChangedFile> = {
  encode(message: ChangedFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      WorkspacePath.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.oldSha256 !== "") {
      writer.uint32(18).string(message.oldSha256);
    }
    if (message.newSha256 !== "") {
      writer.uint32(26).string(message.newSha256);
    }
    if (message.operation !== "") {
      writer.uint32(34).string(message.operation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangedFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangedFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.oldSha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newSha256 = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.operation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ChangedFile>, I>>(base?: I): ChangedFile {
    return ChangedFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChangedFile>, I>>(object: I): ChangedFile {
    const message = createBaseChangedFile();
    message.path = (object.path !== undefined && object.path !== null)
      ? WorkspacePath.fromPartial(object.path)
      : undefined;
    message.oldSha256 = object.oldSha256 ?? "";
    message.newSha256 = object.newSha256 ?? "";
    message.operation = object.operation ?? "";
    return message;
  },
};

function createBaseValidationResult(): ValidationResult {
  return { checkId: "", status: "", summary: "", evidence: undefined };
}

export const ValidationResult: MessageFns<ValidationResult> = {
  encode(message: ValidationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.checkId !== "") {
      writer.uint32(10).string(message.checkId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.summary !== "") {
      writer.uint32(26).string(message.summary);
    }
    if (message.evidence !== undefined) {
      ArtifactRef.encode(message.evidence, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.checkId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.evidence = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ValidationResult>, I>>(base?: I): ValidationResult {
    return ValidationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidationResult>, I>>(object: I): ValidationResult {
    const message = createBaseValidationResult();
    message.checkId = object.checkId ?? "";
    message.status = object.status ?? "";
    message.summary = object.summary ?? "";
    message.evidence = (object.evidence !== undefined && object.evidence !== null)
      ? ArtifactRef.fromPartial(object.evidence)
      : undefined;
    return message;
  },
};

function createBasePatchResponse(): PatchResponse {
  return {
    transactionId: "",
    state: "",
    finalRepositoryRevision: "",
    finalDirtyDigest: "",
    changedFiles: [],
    validations: [],
    completeDiff: undefined,
  };
}

export const PatchResponse: MessageFns<PatchResponse> = {
  encode(message: PatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.transactionId !== "") {
      writer.uint32(10).string(message.transactionId);
    }
    if (message.state !== "") {
      writer.uint32(18).string(message.state);
    }
    if (message.finalRepositoryRevision !== "") {
      writer.uint32(26).string(message.finalRepositoryRevision);
    }
    if (message.finalDirtyDigest !== "") {
      writer.uint32(34).string(message.finalDirtyDigest);
    }
    for (const v of message.changedFiles) {
      ChangedFile.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.validations) {
      ValidationResult.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.completeDiff !== undefined) {
      ArtifactRef.encode(message.completeDiff, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.transactionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.finalRepositoryRevision = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.finalDirtyDigest = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.changedFiles.push(ChangedFile.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.validations.push(ValidationResult.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.completeDiff = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PatchResponse>, I>>(base?: I): PatchResponse {
    return PatchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PatchResponse>, I>>(object: I): PatchResponse {
    const message = createBasePatchResponse();
    message.transactionId = object.transactionId ?? "";
    message.state = object.state ?? "";
    message.finalRepositoryRevision = object.finalRepositoryRevision ?? "";
    message.finalDirtyDigest = object.finalDirtyDigest ?? "";
    message.changedFiles = object.changedFiles?.map((e) => ChangedFile.fromPartial(e)) || [];
    message.validations = object.validations?.map((e) => ValidationResult.fromPartial(e)) || [];
    message.completeDiff = (object.completeDiff !== undefined && object.completeDiff !== null)
      ? ArtifactRef.fromPartial(object.completeDiff)
      : undefined;
    return message;
  },
};

function createBasePatchReconcileRequest(): PatchReconcileRequest {
  return { context: undefined, transactionId: "" };
}

export const PatchReconcileRequest: MessageFns<PatchReconcileRequest> = {
  encode(message: PatchReconcileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.transactionId !== "") {
      writer.uint32(18).string(message.transactionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchReconcileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchReconcileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.transactionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PatchReconcileRequest>, I>>(base?: I): PatchReconcileRequest {
    return PatchReconcileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PatchReconcileRequest>, I>>(object: I): PatchReconcileRequest {
    const message = createBasePatchReconcileRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.transactionId = object.transactionId ?? "";
    return message;
  },
};

function createBaseShellSpec(): ShellSpec {
  return { enabled: false, dialect: "", script: "" };
}

export const ShellSpec: MessageFns<ShellSpec> = {
  encode(message: ShellSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.dialect !== "") {
      writer.uint32(18).string(message.dialect);
    }
    if (message.script !== "") {
      writer.uint32(26).string(message.script);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShellSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShellSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dialect = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.script = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ShellSpec>, I>>(base?: I): ShellSpec {
    return ShellSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShellSpec>, I>>(object: I): ShellSpec {
    const message = createBaseShellSpec();
    message.enabled = object.enabled ?? false;
    message.dialect = object.dialect ?? "";
    message.script = object.script ?? "";
    return message;
  },
};

function createBaseCommandSpec(): CommandSpec {
  return {
    program: "",
    args: [],
    cwd: undefined,
    publicEnv: {},
    secretCapabilityUris: [],
    timeout: undefined,
    allocatePty: false,
    shell: undefined,
  };
}

export const CommandSpec: MessageFns<CommandSpec> = {
  encode(message: CommandSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.program !== "") {
      writer.uint32(10).string(message.program);
    }
    for (const v of message.args) {
      writer.uint32(18).string(v!);
    }
    if (message.cwd !== undefined) {
      WorkspacePath.encode(message.cwd, writer.uint32(26).fork()).join();
    }
    globalThis.Object.entries(message.publicEnv).forEach(([key, value]: [string, string]) => {
      CommandSpec_PublicEnvEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    for (const v of message.secretCapabilityUris) {
      writer.uint32(42).string(v!);
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(50).fork()).join();
    }
    if (message.allocatePty !== false) {
      writer.uint32(56).bool(message.allocatePty);
    }
    if (message.shell !== undefined) {
      ShellSpec.encode(message.shell, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.program = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cwd = WorkspacePath.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = CommandSpec_PublicEnvEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.publicEnv[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secretCapabilityUris.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.allocatePty = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.shell = ShellSpec.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CommandSpec>, I>>(base?: I): CommandSpec {
    return CommandSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandSpec>, I>>(object: I): CommandSpec {
    const message = createBaseCommandSpec();
    message.program = object.program ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.cwd = (object.cwd !== undefined && object.cwd !== null) ? WorkspacePath.fromPartial(object.cwd) : undefined;
    message.publicEnv = (globalThis.Object.entries(object.publicEnv ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.secretCapabilityUris = object.secretCapabilityUris?.map((e) => e) || [];
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    message.allocatePty = object.allocatePty ?? false;
    message.shell = (object.shell !== undefined && object.shell !== null)
      ? ShellSpec.fromPartial(object.shell)
      : undefined;
    return message;
  },
};

function createBaseCommandSpec_PublicEnvEntry(): CommandSpec_PublicEnvEntry {
  return { key: "", value: "" };
}

export const CommandSpec_PublicEnvEntry: MessageFns<CommandSpec_PublicEnvEntry> = {
  encode(message: CommandSpec_PublicEnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandSpec_PublicEnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandSpec_PublicEnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CommandSpec_PublicEnvEntry>, I>>(base?: I): CommandSpec_PublicEnvEntry {
    return CommandSpec_PublicEnvEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandSpec_PublicEnvEntry>, I>>(object: I): CommandSpec_PublicEnvEntry {
    const message = createBaseCommandSpec_PublicEnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseStartProcessRequest(): StartProcessRequest {
  return { context: undefined, intent: undefined, command: undefined, sandboxProfileId: "", outputPolicyId: "" };
}

export const StartProcessRequest: MessageFns<StartProcessRequest> = {
  encode(message: StartProcessRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.intent !== undefined) {
      EffectIntent.encode(message.intent, writer.uint32(18).fork()).join();
    }
    if (message.command !== undefined) {
      CommandSpec.encode(message.command, writer.uint32(26).fork()).join();
    }
    if (message.sandboxProfileId !== "") {
      writer.uint32(34).string(message.sandboxProfileId);
    }
    if (message.outputPolicyId !== "") {
      writer.uint32(42).string(message.outputPolicyId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartProcessRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartProcessRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.intent = EffectIntent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.command = CommandSpec.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sandboxProfileId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.outputPolicyId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StartProcessRequest>, I>>(base?: I): StartProcessRequest {
    return StartProcessRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartProcessRequest>, I>>(object: I): StartProcessRequest {
    const message = createBaseStartProcessRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.intent = (object.intent !== undefined && object.intent !== null)
      ? EffectIntent.fromPartial(object.intent)
      : undefined;
    message.command = (object.command !== undefined && object.command !== null)
      ? CommandSpec.fromPartial(object.command)
      : undefined;
    message.sandboxProfileId = object.sandboxProfileId ?? "";
    message.outputPolicyId = object.outputPolicyId ?? "";
    return message;
  },
};

function createBaseProcessStarted(): ProcessStarted {
  return { processId: "", jobId: "", resolvedExecutable: "", startedAt: undefined };
}

export const ProcessStarted: MessageFns<ProcessStarted> = {
  encode(message: ProcessStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.processId !== "") {
      writer.uint32(10).string(message.processId);
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.resolvedExecutable !== "") {
      writer.uint32(26).string(message.resolvedExecutable);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.processId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resolvedExecutable = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProcessStarted>, I>>(base?: I): ProcessStarted {
    return ProcessStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessStarted>, I>>(object: I): ProcessStarted {
    const message = createBaseProcessStarted();
    message.processId = object.processId ?? "";
    message.jobId = object.jobId ?? "";
    message.resolvedExecutable = object.resolvedExecutable ?? "";
    message.startedAt = object.startedAt ?? undefined;
    return message;
  },
};

function createBaseOutputChunk(): OutputChunk {
  return { cursor: 0, bytes: new Uint8Array(0), redacted: false };
}

export const OutputChunk: MessageFns<OutputChunk> = {
  encode(message: OutputChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cursor !== 0) {
      writer.uint32(8).uint64(message.cursor);
    }
    if (message.bytes.length !== 0) {
      writer.uint32(18).bytes(message.bytes);
    }
    if (message.redacted !== false) {
      writer.uint32(24).bool(message.redacted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OutputChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutputChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cursor = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bytes = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.redacted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<OutputChunk>, I>>(base?: I): OutputChunk {
    return OutputChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OutputChunk>, I>>(object: I): OutputChunk {
    const message = createBaseOutputChunk();
    message.cursor = object.cursor ?? 0;
    message.bytes = object.bytes ?? new Uint8Array(0);
    message.redacted = object.redacted ?? false;
    return message;
  },
};

function createBaseProcessExited(): ProcessExited {
  return { exitCode: 0, signal: "", exitedAt: undefined, stdoutArtifact: undefined, stderrArtifact: undefined };
}

export const ProcessExited: MessageFns<ProcessExited> = {
  encode(message: ProcessExited, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).int32(message.exitCode);
    }
    if (message.signal !== "") {
      writer.uint32(18).string(message.signal);
    }
    if (message.exitedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.exitedAt), writer.uint32(26).fork()).join();
    }
    if (message.stdoutArtifact !== undefined) {
      ArtifactRef.encode(message.stdoutArtifact, writer.uint32(34).fork()).join();
    }
    if (message.stderrArtifact !== undefined) {
      ArtifactRef.encode(message.stderrArtifact, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessExited {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessExited();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signal = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.exitedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.stdoutArtifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.stderrArtifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProcessExited>, I>>(base?: I): ProcessExited {
    return ProcessExited.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessExited>, I>>(object: I): ProcessExited {
    const message = createBaseProcessExited();
    message.exitCode = object.exitCode ?? 0;
    message.signal = object.signal ?? "";
    message.exitedAt = object.exitedAt ?? undefined;
    message.stdoutArtifact = (object.stdoutArtifact !== undefined && object.stdoutArtifact !== null)
      ? ArtifactRef.fromPartial(object.stdoutArtifact)
      : undefined;
    message.stderrArtifact = (object.stderrArtifact !== undefined && object.stderrArtifact !== null)
      ? ArtifactRef.fromPartial(object.stderrArtifact)
      : undefined;
    return message;
  },
};

function createBaseProcessEvent(): ProcessEvent {
  return {
    sequence: 0,
    occurredAt: undefined,
    started: undefined,
    stdout: undefined,
    stderr: undefined,
    exited: undefined,
    policy: undefined,
  };
}

export const ProcessEvent: MessageFns<ProcessEvent> = {
  encode(message: ProcessEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequence !== 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.started !== undefined) {
      ProcessStarted.encode(message.started, writer.uint32(82).fork()).join();
    }
    if (message.stdout !== undefined) {
      OutputChunk.encode(message.stdout, writer.uint32(90).fork()).join();
    }
    if (message.stderr !== undefined) {
      OutputChunk.encode(message.stderr, writer.uint32(98).fork()).join();
    }
    if (message.exited !== undefined) {
      ProcessExited.encode(message.exited, writer.uint32(106).fork()).join();
    }
    if (message.policy !== undefined) {
      PolicyDecision.encode(message.policy, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sequence = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.started = ProcessStarted.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.stdout = OutputChunk.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.stderr = OutputChunk.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.exited = ProcessExited.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.policy = PolicyDecision.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProcessEvent>, I>>(base?: I): ProcessEvent {
    return ProcessEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessEvent>, I>>(object: I): ProcessEvent {
    const message = createBaseProcessEvent();
    message.sequence = object.sequence ?? 0;
    message.occurredAt = object.occurredAt ?? undefined;
    message.started = (object.started !== undefined && object.started !== null)
      ? ProcessStarted.fromPartial(object.started)
      : undefined;
    message.stdout = (object.stdout !== undefined && object.stdout !== null)
      ? OutputChunk.fromPartial(object.stdout)
      : undefined;
    message.stderr = (object.stderr !== undefined && object.stderr !== null)
      ? OutputChunk.fromPartial(object.stderr)
      : undefined;
    message.exited = (object.exited !== undefined && object.exited !== null)
      ? ProcessExited.fromPartial(object.exited)
      : undefined;
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? PolicyDecision.fromPartial(object.policy)
      : undefined;
    return message;
  },
};

function createBaseCancelProcessRequest(): CancelProcessRequest {
  return { context: undefined, processId: "", reason: "" };
}

export const CancelProcessRequest: MessageFns<CancelProcessRequest> = {
  encode(message: CancelProcessRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.processId !== "") {
      writer.uint32(18).string(message.processId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelProcessRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelProcessRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.processId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CancelProcessRequest>, I>>(base?: I): CancelProcessRequest {
    return CancelProcessRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelProcessRequest>, I>>(object: I): CancelProcessRequest {
    const message = createBaseCancelProcessRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.processId = object.processId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseCancelProcessResponse(): CancelProcessResponse {
  return { state: "" };
}

export const CancelProcessResponse: MessageFns<CancelProcessResponse> = {
  encode(message: CancelProcessResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== "") {
      writer.uint32(10).string(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelProcessResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelProcessResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CancelProcessResponse>, I>>(base?: I): CancelProcessResponse {
    return CancelProcessResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelProcessResponse>, I>>(object: I): CancelProcessResponse {
    const message = createBaseCancelProcessResponse();
    message.state = object.state ?? "";
    return message;
  },
};

function createBaseStartJobRequest(): StartJobRequest {
  return {
    context: undefined,
    intent: undefined,
    command: undefined,
    sandboxProfileId: "",
    outputPolicyId: "",
    durable: false,
  };
}

export const StartJobRequest: MessageFns<StartJobRequest> = {
  encode(message: StartJobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.intent !== undefined) {
      EffectIntent.encode(message.intent, writer.uint32(18).fork()).join();
    }
    if (message.command !== undefined) {
      CommandSpec.encode(message.command, writer.uint32(26).fork()).join();
    }
    if (message.sandboxProfileId !== "") {
      writer.uint32(34).string(message.sandboxProfileId);
    }
    if (message.outputPolicyId !== "") {
      writer.uint32(42).string(message.outputPolicyId);
    }
    if (message.durable !== false) {
      writer.uint32(48).bool(message.durable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartJobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.intent = EffectIntent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.command = CommandSpec.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sandboxProfileId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.outputPolicyId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.durable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StartJobRequest>, I>>(base?: I): StartJobRequest {
    return StartJobRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartJobRequest>, I>>(object: I): StartJobRequest {
    const message = createBaseStartJobRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.intent = (object.intent !== undefined && object.intent !== null)
      ? EffectIntent.fromPartial(object.intent)
      : undefined;
    message.command = (object.command !== undefined && object.command !== null)
      ? CommandSpec.fromPartial(object.command)
      : undefined;
    message.sandboxProfileId = object.sandboxProfileId ?? "";
    message.outputPolicyId = object.outputPolicyId ?? "";
    message.durable = object.durable ?? false;
    return message;
  },
};

function createBaseStartJobResponse(): StartJobResponse {
  return { jobId: "", processId: "", startedAt: undefined };
}

export const StartJobResponse: MessageFns<StartJobResponse> = {
  encode(message: StartJobResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.processId !== "") {
      writer.uint32(18).string(message.processId);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartJobResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.processId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StartJobResponse>, I>>(base?: I): StartJobResponse {
    return StartJobResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartJobResponse>, I>>(object: I): StartJobResponse {
    const message = createBaseStartJobResponse();
    message.jobId = object.jobId ?? "";
    message.processId = object.processId ?? "";
    message.startedAt = object.startedAt ?? undefined;
    return message;
  },
};

function createBaseJobStreamRequest(): JobStreamRequest {
  return { context: undefined, jobId: "", fromSequence: 0 };
}

export const JobStreamRequest: MessageFns<JobStreamRequest> = {
  encode(message: JobStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.fromSequence !== 0) {
      writer.uint32(24).uint64(message.fromSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromSequence = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobStreamRequest>, I>>(base?: I): JobStreamRequest {
    return JobStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobStreamRequest>, I>>(object: I): JobStreamRequest {
    const message = createBaseJobStreamRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.jobId = object.jobId ?? "";
    message.fromSequence = object.fromSequence ?? 0;
    return message;
  },
};

function createBaseJobEvent(): JobEvent {
  return {
    sequence: 0,
    occurredAt: undefined,
    started: undefined,
    stdout: undefined,
    stderr: undefined,
    exited: undefined,
    policy: undefined,
    reconciled: undefined,
  };
}

export const JobEvent: MessageFns<JobEvent> = {
  encode(message: JobEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequence !== 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(18).fork()).join();
    }
    if (message.started !== undefined) {
      ProcessStarted.encode(message.started, writer.uint32(82).fork()).join();
    }
    if (message.stdout !== undefined) {
      OutputChunk.encode(message.stdout, writer.uint32(90).fork()).join();
    }
    if (message.stderr !== undefined) {
      OutputChunk.encode(message.stderr, writer.uint32(98).fork()).join();
    }
    if (message.exited !== undefined) {
      ProcessExited.encode(message.exited, writer.uint32(106).fork()).join();
    }
    if (message.policy !== undefined) {
      PolicyDecision.encode(message.policy, writer.uint32(114).fork()).join();
    }
    if (message.reconciled !== undefined) {
      JobReconciled.encode(message.reconciled, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sequence = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.started = ProcessStarted.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.stdout = OutputChunk.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.stderr = OutputChunk.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.exited = ProcessExited.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.policy = PolicyDecision.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.reconciled = JobReconciled.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobEvent>, I>>(base?: I): JobEvent {
    return JobEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobEvent>, I>>(object: I): JobEvent {
    const message = createBaseJobEvent();
    message.sequence = object.sequence ?? 0;
    message.occurredAt = object.occurredAt ?? undefined;
    message.started = (object.started !== undefined && object.started !== null)
      ? ProcessStarted.fromPartial(object.started)
      : undefined;
    message.stdout = (object.stdout !== undefined && object.stdout !== null)
      ? OutputChunk.fromPartial(object.stdout)
      : undefined;
    message.stderr = (object.stderr !== undefined && object.stderr !== null)
      ? OutputChunk.fromPartial(object.stderr)
      : undefined;
    message.exited = (object.exited !== undefined && object.exited !== null)
      ? ProcessExited.fromPartial(object.exited)
      : undefined;
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? PolicyDecision.fromPartial(object.policy)
      : undefined;
    message.reconciled = (object.reconciled !== undefined && object.reconciled !== null)
      ? JobReconciled.fromPartial(object.reconciled)
      : undefined;
    return message;
  },
};

function createBaseJobReconciled(): JobReconciled {
  return { state: "", explanation: "" };
}

export const JobReconciled: MessageFns<JobReconciled> = {
  encode(message: JobReconciled, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== "") {
      writer.uint32(10).string(message.state);
    }
    if (message.explanation !== "") {
      writer.uint32(18).string(message.explanation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobReconciled {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobReconciled();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.explanation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobReconciled>, I>>(base?: I): JobReconciled {
    return JobReconciled.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobReconciled>, I>>(object: I): JobReconciled {
    const message = createBaseJobReconciled();
    message.state = object.state ?? "";
    message.explanation = object.explanation ?? "";
    return message;
  },
};

function createBaseJobInputRequest(): JobInputRequest {
  return { context: undefined, jobId: "", stdin: new Uint8Array(0) };
}

export const JobInputRequest: MessageFns<JobInputRequest> = {
  encode(message: JobInputRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.stdin.length !== 0) {
      writer.uint32(26).bytes(message.stdin);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobInputRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobInputRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stdin = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobInputRequest>, I>>(base?: I): JobInputRequest {
    return JobInputRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobInputRequest>, I>>(object: I): JobInputRequest {
    const message = createBaseJobInputRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.jobId = object.jobId ?? "";
    message.stdin = object.stdin ?? new Uint8Array(0);
    return message;
  },
};

function createBaseJobSignalRequest(): JobSignalRequest {
  return { context: undefined, jobId: "", signal: "" };
}

export const JobSignalRequest: MessageFns<JobSignalRequest> = {
  encode(message: JobSignalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.signal !== "") {
      writer.uint32(26).string(message.signal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobSignalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobSignalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signal = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobSignalRequest>, I>>(base?: I): JobSignalRequest {
    return JobSignalRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobSignalRequest>, I>>(object: I): JobSignalRequest {
    const message = createBaseJobSignalRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.jobId = object.jobId ?? "";
    message.signal = object.signal ?? "";
    return message;
  },
};

function createBaseJobStopRequest(): JobStopRequest {
  return { context: undefined, jobId: "", reason: "" };
}

export const JobStopRequest: MessageFns<JobStopRequest> = {
  encode(message: JobStopRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobStopRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStopRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobStopRequest>, I>>(base?: I): JobStopRequest {
    return JobStopRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobStopRequest>, I>>(object: I): JobStopRequest {
    const message = createBaseJobStopRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.jobId = object.jobId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseJobGetRequest(): JobGetRequest {
  return { context: undefined, jobId: "" };
}

export const JobGetRequest: MessageFns<JobGetRequest> = {
  encode(message: JobGetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobGetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobGetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobGetRequest>, I>>(base?: I): JobGetRequest {
    return JobGetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobGetRequest>, I>>(object: I): JobGetRequest {
    const message = createBaseJobGetRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseJobState(): JobState {
  return {
    jobId: "",
    state: "",
    exitCode: 0,
    startedAt: undefined,
    exitedAt: undefined,
    stdoutArtifact: undefined,
    stderrArtifact: undefined,
  };
}

export const JobState: MessageFns<JobState> = {
  encode(message: JobState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.state !== "") {
      writer.uint32(18).string(message.state);
    }
    if (message.exitCode !== 0) {
      writer.uint32(24).int32(message.exitCode);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(34).fork()).join();
    }
    if (message.exitedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.exitedAt), writer.uint32(42).fork()).join();
    }
    if (message.stdoutArtifact !== undefined) {
      ArtifactRef.encode(message.stdoutArtifact, writer.uint32(50).fork()).join();
    }
    if (message.stderrArtifact !== undefined) {
      ArtifactRef.encode(message.stderrArtifact, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.exitedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stdoutArtifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.stderrArtifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<JobState>, I>>(base?: I): JobState {
    return JobState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobState>, I>>(object: I): JobState {
    const message = createBaseJobState();
    message.jobId = object.jobId ?? "";
    message.state = object.state ?? "";
    message.exitCode = object.exitCode ?? 0;
    message.startedAt = object.startedAt ?? undefined;
    message.exitedAt = object.exitedAt ?? undefined;
    message.stdoutArtifact = (object.stdoutArtifact !== undefined && object.stdoutArtifact !== null)
      ? ArtifactRef.fromPartial(object.stdoutArtifact)
      : undefined;
    message.stderrArtifact = (object.stderrArtifact !== undefined && object.stderrArtifact !== null)
      ? ArtifactRef.fromPartial(object.stderrArtifact)
      : undefined;
    return message;
  },
};

function createBaseKernelInfo(): KernelInfo {
  return {
    version: "",
    protocolVersion: "",
    buildRevision: "",
    supportedBackends: [],
    supportedServices: [],
    instanceId: "",
  };
}

export const KernelInfo: MessageFns<KernelInfo> = {
  encode(message: KernelInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.protocolVersion !== "") {
      writer.uint32(18).string(message.protocolVersion);
    }
    if (message.buildRevision !== "") {
      writer.uint32(26).string(message.buildRevision);
    }
    for (const v of message.supportedBackends) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.supportedServices) {
      writer.uint32(42).string(v!);
    }
    if (message.instanceId !== "") {
      writer.uint32(50).string(message.instanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KernelInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKernelInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.protocolVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.buildRevision = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.supportedBackends.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.supportedServices.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KernelInfo>, I>>(base?: I): KernelInfo {
    return KernelInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KernelInfo>, I>>(object: I): KernelInfo {
    const message = createBaseKernelInfo();
    message.version = object.version ?? "";
    message.protocolVersion = object.protocolVersion ?? "";
    message.buildRevision = object.buildRevision ?? "";
    message.supportedBackends = object.supportedBackends?.map((e) => e) || [];
    message.supportedServices = object.supportedServices?.map((e) => e) || [];
    message.instanceId = object.instanceId ?? "";
    return message;
  },
};

function createBaseKernelHealth(): KernelHealth {
  return { state: "", degradations: [], checkedAt: undefined };
}

export const KernelHealth: MessageFns<KernelHealth> = {
  encode(message: KernelHealth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== "") {
      writer.uint32(10).string(message.state);
    }
    for (const v of message.degradations) {
      writer.uint32(18).string(v!);
    }
    if (message.checkedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.checkedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KernelHealth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKernelHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.degradations.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.checkedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KernelHealth>, I>>(base?: I): KernelHealth {
    return KernelHealth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KernelHealth>, I>>(object: I): KernelHealth {
    const message = createBaseKernelHealth();
    message.state = object.state ?? "";
    message.degradations = object.degradations?.map((e) => e) || [];
    message.checkedAt = object.checkedAt ?? undefined;
    return message;
  },
};

function createBaseBootstrapControlRequest(): BootstrapControlRequest {
  return { principal: "" };
}

export const BootstrapControlRequest: MessageFns<BootstrapControlRequest> = {
  encode(message: BootstrapControlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.principal !== "") {
      writer.uint32(10).string(message.principal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BootstrapControlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBootstrapControlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.principal = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BootstrapControlRequest>, I>>(base?: I): BootstrapControlRequest {
    return BootstrapControlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BootstrapControlRequest>, I>>(object: I): BootstrapControlRequest {
    const message = createBaseBootstrapControlRequest();
    message.principal = object.principal ?? "";
    return message;
  },
};

function createBaseBootstrapControlCapabilities(): BootstrapControlCapabilities {
  return { brokerCapabilityToken: "", maintenanceCapabilityToken: "", expiresAtUnix: 0 };
}

export const BootstrapControlCapabilities: MessageFns<BootstrapControlCapabilities> = {
  encode(message: BootstrapControlCapabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.brokerCapabilityToken !== "") {
      writer.uint32(10).string(message.brokerCapabilityToken);
    }
    if (message.maintenanceCapabilityToken !== "") {
      writer.uint32(18).string(message.maintenanceCapabilityToken);
    }
    if (message.expiresAtUnix !== 0) {
      writer.uint32(24).uint64(message.expiresAtUnix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BootstrapControlCapabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBootstrapControlCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.brokerCapabilityToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.maintenanceCapabilityToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiresAtUnix = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BootstrapControlCapabilities>, I>>(base?: I): BootstrapControlCapabilities {
    return BootstrapControlCapabilities.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BootstrapControlCapabilities>, I>>(object: I): BootstrapControlCapabilities {
    const message = createBaseBootstrapControlCapabilities();
    message.brokerCapabilityToken = object.brokerCapabilityToken ?? "";
    message.maintenanceCapabilityToken = object.maintenanceCapabilityToken ?? "";
    message.expiresAtUnix = object.expiresAtUnix ?? 0;
    return message;
  },
};

function createBaseRegisterWorkspaceRequest(): RegisterWorkspaceRequest {
  return {
    context: undefined,
    rootUri: "",
    canonicalRoot: "",
    trust: "",
    remoteEnvironmentJson: "",
    kind: "",
    requestedWorkspaceId: "",
  };
}

export const RegisterWorkspaceRequest: MessageFns<RegisterWorkspaceRequest> = {
  encode(message: RegisterWorkspaceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.rootUri !== "") {
      writer.uint32(18).string(message.rootUri);
    }
    if (message.canonicalRoot !== "") {
      writer.uint32(26).string(message.canonicalRoot);
    }
    if (message.trust !== "") {
      writer.uint32(34).string(message.trust);
    }
    if (message.remoteEnvironmentJson !== "") {
      writer.uint32(42).string(message.remoteEnvironmentJson);
    }
    if (message.kind !== "") {
      writer.uint32(50).string(message.kind);
    }
    if (message.requestedWorkspaceId !== "") {
      writer.uint32(58).string(message.requestedWorkspaceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkspaceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkspaceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rootUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.canonicalRoot = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.trust = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.remoteEnvironmentJson = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.requestedWorkspaceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RegisterWorkspaceRequest>, I>>(base?: I): RegisterWorkspaceRequest {
    return RegisterWorkspaceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkspaceRequest>, I>>(object: I): RegisterWorkspaceRequest {
    const message = createBaseRegisterWorkspaceRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.rootUri = object.rootUri ?? "";
    message.canonicalRoot = object.canonicalRoot ?? "";
    message.trust = object.trust ?? "";
    message.remoteEnvironmentJson = object.remoteEnvironmentJson ?? "";
    message.kind = object.kind ?? "";
    message.requestedWorkspaceId = object.requestedWorkspaceId ?? "";
    return message;
  },
};

function createBaseWorkspaceEntryMessage(): WorkspaceEntryMessage {
  return { id: "", rootUri: "", canonicalRoot: "", trust: "" };
}

export const WorkspaceEntryMessage: MessageFns<WorkspaceEntryMessage> = {
  encode(message: WorkspaceEntryMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.rootUri !== "") {
      writer.uint32(18).string(message.rootUri);
    }
    if (message.canonicalRoot !== "") {
      writer.uint32(26).string(message.canonicalRoot);
    }
    if (message.trust !== "") {
      writer.uint32(34).string(message.trust);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceEntryMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceEntryMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rootUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.canonicalRoot = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.trust = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkspaceEntryMessage>, I>>(base?: I): WorkspaceEntryMessage {
    return WorkspaceEntryMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkspaceEntryMessage>, I>>(object: I): WorkspaceEntryMessage {
    const message = createBaseWorkspaceEntryMessage();
    message.id = object.id ?? "";
    message.rootUri = object.rootUri ?? "";
    message.canonicalRoot = object.canonicalRoot ?? "";
    message.trust = object.trust ?? "";
    return message;
  },
};

function createBaseResolveWorkspaceRootRequest(): ResolveWorkspaceRootRequest {
  return { context: undefined, rootUri: "", candidateRoot: "" };
}

export const ResolveWorkspaceRootRequest: MessageFns<ResolveWorkspaceRootRequest> = {
  encode(message: ResolveWorkspaceRootRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.rootUri !== "") {
      writer.uint32(18).string(message.rootUri);
    }
    if (message.candidateRoot !== "") {
      writer.uint32(26).string(message.candidateRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveWorkspaceRootRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveWorkspaceRootRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rootUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.candidateRoot = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveWorkspaceRootRequest>, I>>(base?: I): ResolveWorkspaceRootRequest {
    return ResolveWorkspaceRootRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveWorkspaceRootRequest>, I>>(object: I): ResolveWorkspaceRootRequest {
    const message = createBaseResolveWorkspaceRootRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.rootUri = object.rootUri ?? "";
    message.candidateRoot = object.candidateRoot ?? "";
    return message;
  },
};

function createBaseResolvedWorkspaceRootMessage(): ResolvedWorkspaceRootMessage {
  return { rootUri: "", canonicalRoot: "" };
}

export const ResolvedWorkspaceRootMessage: MessageFns<ResolvedWorkspaceRootMessage> = {
  encode(message: ResolvedWorkspaceRootMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rootUri !== "") {
      writer.uint32(10).string(message.rootUri);
    }
    if (message.canonicalRoot !== "") {
      writer.uint32(18).string(message.canonicalRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolvedWorkspaceRootMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolvedWorkspaceRootMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rootUri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.canonicalRoot = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolvedWorkspaceRootMessage>, I>>(base?: I): ResolvedWorkspaceRootMessage {
    return ResolvedWorkspaceRootMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolvedWorkspaceRootMessage>, I>>(object: I): ResolvedWorkspaceRootMessage {
    const message = createBaseResolvedWorkspaceRootMessage();
    message.rootUri = object.rootUri ?? "";
    message.canonicalRoot = object.canonicalRoot ?? "";
    return message;
  },
};

function createBaseGetWorkspaceRequest(): GetWorkspaceRequest {
  return { context: undefined, workspaceId: "" };
}

export const GetWorkspaceRequest: MessageFns<GetWorkspaceRequest> = {
  encode(message: GetWorkspaceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWorkspaceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWorkspaceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetWorkspaceRequest>, I>>(base?: I): GetWorkspaceRequest {
    return GetWorkspaceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetWorkspaceRequest>, I>>(object: I): GetWorkspaceRequest {
    const message = createBaseGetWorkspaceRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.workspaceId = object.workspaceId ?? "";
    return message;
  },
};

function createBaseEnforcementReportMessage(): EnforcementReportMessage {
  return { backendId: "", status: 0, enforced: [], degraded: [], unsupported: [], notes: [] };
}

export const EnforcementReportMessage: MessageFns<EnforcementReportMessage> = {
  encode(message: EnforcementReportMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.backendId !== "") {
      writer.uint32(10).string(message.backendId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    for (const v of message.enforced) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.degraded) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.unsupported) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.notes) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnforcementReportMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnforcementReportMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.backendId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.enforced.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.degraded.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.unsupported.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.notes.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EnforcementReportMessage>, I>>(base?: I): EnforcementReportMessage {
    return EnforcementReportMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EnforcementReportMessage>, I>>(object: I): EnforcementReportMessage {
    const message = createBaseEnforcementReportMessage();
    message.backendId = object.backendId ?? "";
    message.status = object.status ?? 0;
    message.enforced = object.enforced?.map((e) => e) || [];
    message.degraded = object.degraded?.map((e) => e) || [];
    message.unsupported = object.unsupported?.map((e) => e) || [];
    message.notes = object.notes?.map((e) => e) || [];
    return message;
  },
};

function createBaseSandboxReportRequest(): SandboxReportRequest {
  return { context: undefined, profileId: "" };
}

export const SandboxReportRequest: MessageFns<SandboxReportRequest> = {
  encode(message: SandboxReportRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.profileId !== "") {
      writer.uint32(18).string(message.profileId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxReportRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxReportRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.profileId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SandboxReportRequest>, I>>(base?: I): SandboxReportRequest {
    return SandboxReportRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxReportRequest>, I>>(object: I): SandboxReportRequest {
    const message = createBaseSandboxReportRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.profileId = object.profileId ?? "";
    return message;
  },
};

function createBaseNormalizedCommandMessage(): NormalizedCommandMessage {
  return { resolvedExecutable: "", argv: [], workingDirectory: "", effectTypes: [], taintSources: [] };
}

export const NormalizedCommandMessage: MessageFns<NormalizedCommandMessage> = {
  encode(message: NormalizedCommandMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resolvedExecutable !== "") {
      writer.uint32(10).string(message.resolvedExecutable);
    }
    for (const v of message.argv) {
      writer.uint32(18).string(v!);
    }
    if (message.workingDirectory !== "") {
      writer.uint32(26).string(message.workingDirectory);
    }
    for (const v of message.effectTypes) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.taintSources) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NormalizedCommandMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNormalizedCommandMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resolvedExecutable = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.argv.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workingDirectory = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.effectTypes.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.taintSources.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<NormalizedCommandMessage>, I>>(base?: I): NormalizedCommandMessage {
    return NormalizedCommandMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NormalizedCommandMessage>, I>>(object: I): NormalizedCommandMessage {
    const message = createBaseNormalizedCommandMessage();
    message.resolvedExecutable = object.resolvedExecutable ?? "";
    message.argv = object.argv?.map((e) => e) || [];
    message.workingDirectory = object.workingDirectory ?? "";
    message.effectTypes = object.effectTypes?.map((e) => e) || [];
    message.taintSources = object.taintSources?.map((e) => e) || [];
    return message;
  },
};

function createBaseConstraintMessage(): ConstraintMessage {
  return { maxRuntimeMs: undefined, maxOutputBytes: undefined, disallowedEnv: [] };
}

export const ConstraintMessage: MessageFns<ConstraintMessage> = {
  encode(message: ConstraintMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxRuntimeMs !== undefined) {
      writer.uint32(8).uint64(message.maxRuntimeMs);
    }
    if (message.maxOutputBytes !== undefined) {
      writer.uint32(16).uint64(message.maxOutputBytes);
    }
    for (const v of message.disallowedEnv) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConstraintMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConstraintMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxRuntimeMs = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxOutputBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.disallowedEnv.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConstraintMessage>, I>>(base?: I): ConstraintMessage {
    return ConstraintMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConstraintMessage>, I>>(object: I): ConstraintMessage {
    const message = createBaseConstraintMessage();
    message.maxRuntimeMs = object.maxRuntimeMs ?? undefined;
    message.maxOutputBytes = object.maxOutputBytes ?? undefined;
    message.disallowedEnv = object.disallowedEnv?.map((e) => e) || [];
    return message;
  },
};

function createBaseDecisionReportMessage(): DecisionReportMessage {
  return { decision: 0, ruleIds: [], explanation: "", decisionId: "", constraints: undefined };
}

export const DecisionReportMessage: MessageFns<DecisionReportMessage> = {
  encode(message: DecisionReportMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.decision !== 0) {
      writer.uint32(8).int32(message.decision);
    }
    for (const v of message.ruleIds) {
      writer.uint32(18).string(v!);
    }
    if (message.explanation !== "") {
      writer.uint32(26).string(message.explanation);
    }
    if (message.decisionId !== "") {
      writer.uint32(34).string(message.decisionId);
    }
    if (message.constraints !== undefined) {
      ConstraintMessage.encode(message.constraints, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DecisionReportMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDecisionReportMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.decision = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ruleIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.explanation = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.decisionId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.constraints = ConstraintMessage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<DecisionReportMessage>, I>>(base?: I): DecisionReportMessage {
    return DecisionReportMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DecisionReportMessage>, I>>(object: I): DecisionReportMessage {
    const message = createBaseDecisionReportMessage();
    message.decision = object.decision ?? 0;
    message.ruleIds = object.ruleIds?.map((e) => e) || [];
    message.explanation = object.explanation ?? "";
    message.decisionId = object.decisionId ?? "";
    message.constraints = (object.constraints !== undefined && object.constraints !== null)
      ? ConstraintMessage.fromPartial(object.constraints)
      : undefined;
    return message;
  },
};

function createBaseEvaluatePolicyRequest(): EvaluatePolicyRequest {
  return { context: undefined, command: undefined };
}

export const EvaluatePolicyRequest: MessageFns<EvaluatePolicyRequest> = {
  encode(message: EvaluatePolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.command !== undefined) {
      NormalizedCommandMessage.encode(message.command, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluatePolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluatePolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command = NormalizedCommandMessage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EvaluatePolicyRequest>, I>>(base?: I): EvaluatePolicyRequest {
    return EvaluatePolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EvaluatePolicyRequest>, I>>(object: I): EvaluatePolicyRequest {
    const message = createBaseEvaluatePolicyRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.command = (object.command !== undefined && object.command !== null)
      ? NormalizedCommandMessage.fromPartial(object.command)
      : undefined;
    return message;
  },
};

function createBaseMintTaskCapabilityRequest(): MintTaskCapabilityRequest {
  return {
    context: undefined,
    principal: "",
    sessionId: "",
    taskId: "",
    workspaceId: "",
    operationClasses: [],
    workspacePaths: [],
    networkDestinations: [],
    secretCapabilities: [],
    ttlSeconds: 0,
  };
}

export const MintTaskCapabilityRequest: MessageFns<MintTaskCapabilityRequest> = {
  encode(message: MintTaskCapabilityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.principal !== "") {
      writer.uint32(18).string(message.principal);
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    if (message.taskId !== "") {
      writer.uint32(34).string(message.taskId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(42).string(message.workspaceId);
    }
    writer.uint32(50).fork();
    for (const v of message.operationClasses) {
      writer.int32(v);
    }
    writer.join();
    for (const v of message.workspacePaths) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.networkDestinations) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.secretCapabilities) {
      writer.uint32(74).string(v!);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(80).uint64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MintTaskCapabilityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMintTaskCapabilityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.principal = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.operationClasses.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.operationClasses.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.workspacePaths.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.networkDestinations.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.secretCapabilities.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<MintTaskCapabilityRequest>, I>>(base?: I): MintTaskCapabilityRequest {
    return MintTaskCapabilityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MintTaskCapabilityRequest>, I>>(object: I): MintTaskCapabilityRequest {
    const message = createBaseMintTaskCapabilityRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.principal = object.principal ?? "";
    message.sessionId = object.sessionId ?? "";
    message.taskId = object.taskId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.operationClasses = object.operationClasses?.map((e) => e) || [];
    message.workspacePaths = object.workspacePaths?.map((e) => e) || [];
    message.networkDestinations = object.networkDestinations?.map((e) => e) || [];
    message.secretCapabilities = object.secretCapabilities?.map((e) => e) || [];
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseMintTaskCapabilityResponse(): MintTaskCapabilityResponse {
  return { capabilityToken: "", expiresAtUnix: 0 };
}

export const MintTaskCapabilityResponse: MessageFns<MintTaskCapabilityResponse> = {
  encode(message: MintTaskCapabilityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capabilityToken !== "") {
      writer.uint32(10).string(message.capabilityToken);
    }
    if (message.expiresAtUnix !== 0) {
      writer.uint32(16).uint64(message.expiresAtUnix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MintTaskCapabilityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMintTaskCapabilityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.capabilityToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expiresAtUnix = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<MintTaskCapabilityResponse>, I>>(base?: I): MintTaskCapabilityResponse {
    return MintTaskCapabilityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MintTaskCapabilityResponse>, I>>(object: I): MintTaskCapabilityResponse {
    const message = createBaseMintTaskCapabilityResponse();
    message.capabilityToken = object.capabilityToken ?? "";
    message.expiresAtUnix = object.expiresAtUnix ?? 0;
    return message;
  },
};

function createBaseMintSecretRequest(): MintSecretRequest {
  return { context: undefined, capabilityUri: "", ttlSeconds: 0 };
}

export const MintSecretRequest: MessageFns<MintSecretRequest> = {
  encode(message: MintSecretRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.capabilityUri !== "") {
      writer.uint32(18).string(message.capabilityUri);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(24).uint64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MintSecretRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMintSecretRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilityUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<MintSecretRequest>, I>>(base?: I): MintSecretRequest {
    return MintSecretRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MintSecretRequest>, I>>(object: I): MintSecretRequest {
    const message = createBaseMintSecretRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.capabilityUri = object.capabilityUri ?? "";
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseSecretCapabilityMessage(): SecretCapabilityMessage {
  return { capabilityUri: "", handle: "", expiresAtUnix: 0 };
}

export const SecretCapabilityMessage: MessageFns<SecretCapabilityMessage> = {
  encode(message: SecretCapabilityMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capabilityUri !== "") {
      writer.uint32(10).string(message.capabilityUri);
    }
    if (message.handle !== "") {
      writer.uint32(18).string(message.handle);
    }
    if (message.expiresAtUnix !== 0) {
      writer.uint32(24).uint64(message.expiresAtUnix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecretCapabilityMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecretCapabilityMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.capabilityUri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.handle = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiresAtUnix = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SecretCapabilityMessage>, I>>(base?: I): SecretCapabilityMessage {
    return SecretCapabilityMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecretCapabilityMessage>, I>>(object: I): SecretCapabilityMessage {
    const message = createBaseSecretCapabilityMessage();
    message.capabilityUri = object.capabilityUri ?? "";
    message.handle = object.handle ?? "";
    message.expiresAtUnix = object.expiresAtUnix ?? 0;
    return message;
  },
};

function createBaseStoreSecretRequest(): StoreSecretRequest {
  return { context: undefined, capabilityUri: "", value: new Uint8Array(0) };
}

export const StoreSecretRequest: MessageFns<StoreSecretRequest> = {
  encode(message: StoreSecretRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.capabilityUri !== "") {
      writer.uint32(18).string(message.capabilityUri);
    }
    if (message.value.length !== 0) {
      writer.uint32(26).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoreSecretRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoreSecretRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilityUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StoreSecretRequest>, I>>(base?: I): StoreSecretRequest {
    return StoreSecretRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoreSecretRequest>, I>>(object: I): StoreSecretRequest {
    const message = createBaseStoreSecretRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.capabilityUri = object.capabilityUri ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseDeleteSecretRequest(): DeleteSecretRequest {
  return { context: undefined, capabilityUri: "" };
}

export const DeleteSecretRequest: MessageFns<DeleteSecretRequest> = {
  encode(message: DeleteSecretRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.capabilityUri !== "") {
      writer.uint32(18).string(message.capabilityUri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSecretRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSecretRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilityUri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<DeleteSecretRequest>, I>>(base?: I): DeleteSecretRequest {
    return DeleteSecretRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSecretRequest>, I>>(object: I): DeleteSecretRequest {
    const message = createBaseDeleteSecretRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.capabilityUri = object.capabilityUri ?? "";
    return message;
  },
};

function createBaseSecretMutationResponse(): SecretMutationResponse {
  return { capabilityUri: "", stored: false };
}

export const SecretMutationResponse: MessageFns<SecretMutationResponse> = {
  encode(message: SecretMutationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capabilityUri !== "") {
      writer.uint32(10).string(message.capabilityUri);
    }
    if (message.stored !== false) {
      writer.uint32(16).bool(message.stored);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecretMutationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecretMutationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.capabilityUri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stored = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SecretMutationResponse>, I>>(base?: I): SecretMutationResponse {
    return SecretMutationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SecretMutationResponse>, I>>(object: I): SecretMutationResponse {
    const message = createBaseSecretMutationResponse();
    message.capabilityUri = object.capabilityUri ?? "";
    message.stored = object.stored ?? false;
    return message;
  },
};

function createBaseEgressRequest(): EgressRequest {
  return { context: undefined, destination: "", method: "" };
}

export const EgressRequest: MessageFns<EgressRequest> = {
  encode(message: EgressRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.destination !== "") {
      writer.uint32(18).string(message.destination);
    }
    if (message.method !== "") {
      writer.uint32(26).string(message.method);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EgressRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEgressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.destination = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.method = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EgressRequest>, I>>(base?: I): EgressRequest {
    return EgressRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EgressRequest>, I>>(object: I): EgressRequest {
    const message = createBaseEgressRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.destination = object.destination ?? "";
    message.method = object.method ?? "";
    return message;
  },
};

function createBaseEgressDecisionMessage(): EgressDecisionMessage {
  return { allowed: false, reason: "" };
}

export const EgressDecisionMessage: MessageFns<EgressDecisionMessage> = {
  encode(message: EgressDecisionMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allowed !== false) {
      writer.uint32(8).bool(message.allowed);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EgressDecisionMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEgressDecisionMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.allowed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EgressDecisionMessage>, I>>(base?: I): EgressDecisionMessage {
    return EgressDecisionMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EgressDecisionMessage>, I>>(object: I): EgressDecisionMessage {
    const message = createBaseEgressDecisionMessage();
    message.allowed = object.allowed ?? false;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseConnectorGrantBindingMessage(): ConnectorGrantBindingMessage {
  return {
    connectorId: "",
    destinationHost: "",
    destinationPort: 0,
    scheme: "",
    method: "",
    pathClass: "",
    effectId: "",
  };
}

export const ConnectorGrantBindingMessage: MessageFns<ConnectorGrantBindingMessage> = {
  encode(message: ConnectorGrantBindingMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.connectorId !== "") {
      writer.uint32(10).string(message.connectorId);
    }
    if (message.destinationHost !== "") {
      writer.uint32(18).string(message.destinationHost);
    }
    if (message.destinationPort !== 0) {
      writer.uint32(24).uint32(message.destinationPort);
    }
    if (message.scheme !== "") {
      writer.uint32(34).string(message.scheme);
    }
    if (message.method !== "") {
      writer.uint32(42).string(message.method);
    }
    if (message.pathClass !== "") {
      writer.uint32(50).string(message.pathClass);
    }
    if (message.effectId !== "") {
      writer.uint32(58).string(message.effectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorGrantBindingMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorGrantBindingMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.connectorId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.destinationHost = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.destinationPort = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.pathClass = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.effectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConnectorGrantBindingMessage>, I>>(base?: I): ConnectorGrantBindingMessage {
    return ConnectorGrantBindingMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorGrantBindingMessage>, I>>(object: I): ConnectorGrantBindingMessage {
    const message = createBaseConnectorGrantBindingMessage();
    message.connectorId = object.connectorId ?? "";
    message.destinationHost = object.destinationHost ?? "";
    message.destinationPort = object.destinationPort ?? 0;
    message.scheme = object.scheme ?? "";
    message.method = object.method ?? "";
    message.pathClass = object.pathClass ?? "";
    message.effectId = object.effectId ?? "";
    return message;
  },
};

function createBaseMintConnectorGrantRequest(): MintConnectorGrantRequest {
  return { context: undefined, capabilityUri: "", binding: undefined, ttlSeconds: 0 };
}

export const MintConnectorGrantRequest: MessageFns<MintConnectorGrantRequest> = {
  encode(message: MintConnectorGrantRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.capabilityUri !== "") {
      writer.uint32(18).string(message.capabilityUri);
    }
    if (message.binding !== undefined) {
      ConnectorGrantBindingMessage.encode(message.binding, writer.uint32(26).fork()).join();
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(32).uint64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MintConnectorGrantRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMintConnectorGrantRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilityUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binding = ConnectorGrantBindingMessage.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<MintConnectorGrantRequest>, I>>(base?: I): MintConnectorGrantRequest {
    return MintConnectorGrantRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MintConnectorGrantRequest>, I>>(object: I): MintConnectorGrantRequest {
    const message = createBaseMintConnectorGrantRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.capabilityUri = object.capabilityUri ?? "";
    message.binding = (object.binding !== undefined && object.binding !== null)
      ? ConnectorGrantBindingMessage.fromPartial(object.binding)
      : undefined;
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseConnectorGrantMessage(): ConnectorGrantMessage {
  return { encodedGrant: "", grantId: "", expiresAtUnix: 0 };
}

export const ConnectorGrantMessage: MessageFns<ConnectorGrantMessage> = {
  encode(message: ConnectorGrantMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.encodedGrant !== "") {
      writer.uint32(10).string(message.encodedGrant);
    }
    if (message.grantId !== "") {
      writer.uint32(18).string(message.grantId);
    }
    if (message.expiresAtUnix !== 0) {
      writer.uint32(24).uint64(message.expiresAtUnix);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorGrantMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorGrantMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.encodedGrant = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.grantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiresAtUnix = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConnectorGrantMessage>, I>>(base?: I): ConnectorGrantMessage {
    return ConnectorGrantMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorGrantMessage>, I>>(object: I): ConnectorGrantMessage {
    const message = createBaseConnectorGrantMessage();
    message.encodedGrant = object.encodedGrant ?? "";
    message.grantId = object.grantId ?? "";
    message.expiresAtUnix = object.expiresAtUnix ?? 0;
    return message;
  },
};

function createBaseConnectorHeaderMessage(): ConnectorHeaderMessage {
  return { name: "", value: "" };
}

export const ConnectorHeaderMessage: MessageFns<ConnectorHeaderMessage> = {
  encode(message: ConnectorHeaderMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorHeaderMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorHeaderMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConnectorHeaderMessage>, I>>(base?: I): ConnectorHeaderMessage {
    return ConnectorHeaderMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorHeaderMessage>, I>>(object: I): ConnectorHeaderMessage {
    const message = createBaseConnectorHeaderMessage();
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseConnectorOperationMessage(): ConnectorOperationMessage {
  return { method: "", scheme: "", host: "", port: 0, path: "", query: "", headers: [], body: new Uint8Array(0) };
}

export const ConnectorOperationMessage: MessageFns<ConnectorOperationMessage> = {
  encode(message: ConnectorOperationMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    if (message.scheme !== "") {
      writer.uint32(18).string(message.scheme);
    }
    if (message.host !== "") {
      writer.uint32(26).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(32).uint32(message.port);
    }
    if (message.path !== "") {
      writer.uint32(42).string(message.path);
    }
    if (message.query !== "") {
      writer.uint32(50).string(message.query);
    }
    for (const v of message.headers) {
      ConnectorHeaderMessage.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.body.length !== 0) {
      writer.uint32(66).bytes(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorOperationMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorOperationMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.headers.push(ConnectorHeaderMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConnectorOperationMessage>, I>>(base?: I): ConnectorOperationMessage {
    return ConnectorOperationMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorOperationMessage>, I>>(object: I): ConnectorOperationMessage {
    const message = createBaseConnectorOperationMessage();
    message.method = object.method ?? "";
    message.scheme = object.scheme ?? "";
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.path = object.path ?? "";
    message.query = object.query ?? "";
    message.headers = object.headers?.map((e) => ConnectorHeaderMessage.fromPartial(e)) || [];
    message.body = object.body ?? new Uint8Array(0);
    return message;
  },
};

function createBaseExecuteConnectorRequest(): ExecuteConnectorRequest {
  return { context: undefined, encodedGrant: "", operation: undefined };
}

export const ExecuteConnectorRequest: MessageFns<ExecuteConnectorRequest> = {
  encode(message: ExecuteConnectorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.encodedGrant !== "") {
      writer.uint32(18).string(message.encodedGrant);
    }
    if (message.operation !== undefined) {
      ConnectorOperationMessage.encode(message.operation, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteConnectorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteConnectorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.encodedGrant = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.operation = ConnectorOperationMessage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ExecuteConnectorRequest>, I>>(base?: I): ExecuteConnectorRequest {
    return ExecuteConnectorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteConnectorRequest>, I>>(object: I): ExecuteConnectorRequest {
    const message = createBaseExecuteConnectorRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.encodedGrant = object.encodedGrant ?? "";
    message.operation = (object.operation !== undefined && object.operation !== null)
      ? ConnectorOperationMessage.fromPartial(object.operation)
      : undefined;
    return message;
  },
};

function createBaseConnectorReceiptMessage(): ConnectorReceiptMessage {
  return {
    grantId: "",
    taskId: "",
    effectId: "",
    connectorId: "",
    method: "",
    path: "",
    destination: "",
    requestSha256: "",
    statusCode: undefined,
    responseSha256: undefined,
    responseRedactions: 0,
    outcome: "",
  };
}

export const ConnectorReceiptMessage: MessageFns<ConnectorReceiptMessage> = {
  encode(message: ConnectorReceiptMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grantId !== "") {
      writer.uint32(10).string(message.grantId);
    }
    if (message.taskId !== "") {
      writer.uint32(18).string(message.taskId);
    }
    if (message.effectId !== "") {
      writer.uint32(26).string(message.effectId);
    }
    if (message.connectorId !== "") {
      writer.uint32(34).string(message.connectorId);
    }
    if (message.method !== "") {
      writer.uint32(42).string(message.method);
    }
    if (message.path !== "") {
      writer.uint32(50).string(message.path);
    }
    if (message.destination !== "") {
      writer.uint32(58).string(message.destination);
    }
    if (message.requestSha256 !== "") {
      writer.uint32(66).string(message.requestSha256);
    }
    if (message.statusCode !== undefined) {
      writer.uint32(72).uint32(message.statusCode);
    }
    if (message.responseSha256 !== undefined) {
      writer.uint32(82).string(message.responseSha256);
    }
    if (message.responseRedactions !== 0) {
      writer.uint32(88).uint64(message.responseRedactions);
    }
    if (message.outcome !== "") {
      writer.uint32(98).string(message.outcome);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorReceiptMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorReceiptMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.grantId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.effectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.connectorId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.destination = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.requestSha256 = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.statusCode = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.responseSha256 = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.responseRedactions = longToNumber(reader.uint64());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.outcome = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConnectorReceiptMessage>, I>>(base?: I): ConnectorReceiptMessage {
    return ConnectorReceiptMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorReceiptMessage>, I>>(object: I): ConnectorReceiptMessage {
    const message = createBaseConnectorReceiptMessage();
    message.grantId = object.grantId ?? "";
    message.taskId = object.taskId ?? "";
    message.effectId = object.effectId ?? "";
    message.connectorId = object.connectorId ?? "";
    message.method = object.method ?? "";
    message.path = object.path ?? "";
    message.destination = object.destination ?? "";
    message.requestSha256 = object.requestSha256 ?? "";
    message.statusCode = object.statusCode ?? undefined;
    message.responseSha256 = object.responseSha256 ?? undefined;
    message.responseRedactions = object.responseRedactions ?? 0;
    message.outcome = object.outcome ?? "";
    return message;
  },
};

function createBaseConnectorResponseMessage(): ConnectorResponseMessage {
  return { receipt: undefined, body: new Uint8Array(0), contentType: undefined };
}

export const ConnectorResponseMessage: MessageFns<ConnectorResponseMessage> = {
  encode(message: ConnectorResponseMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.receipt !== undefined) {
      ConnectorReceiptMessage.encode(message.receipt, writer.uint32(10).fork()).join();
    }
    if (message.body.length !== 0) {
      writer.uint32(18).bytes(message.body);
    }
    if (message.contentType !== undefined) {
      writer.uint32(26).string(message.contentType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectorResponseMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorResponseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.receipt = ConnectorReceiptMessage.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConnectorResponseMessage>, I>>(base?: I): ConnectorResponseMessage {
    return ConnectorResponseMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorResponseMessage>, I>>(object: I): ConnectorResponseMessage {
    const message = createBaseConnectorResponseMessage();
    message.receipt = (object.receipt !== undefined && object.receipt !== null)
      ? ConnectorReceiptMessage.fromPartial(object.receipt)
      : undefined;
    message.body = object.body ?? new Uint8Array(0);
    message.contentType = object.contentType ?? undefined;
    return message;
  },
};

function createBaseCodeSearchRequest(): CodeSearchRequest {
  return { context: undefined, workspaceId: "", query: "", limit: 0 };
}

export const CodeSearchRequest: MessageFns<CodeSearchRequest> = {
  encode(message: CodeSearchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.query !== "") {
      writer.uint32(26).string(message.query);
    }
    if (message.limit !== 0) {
      writer.uint32(32).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeSearchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodeSearchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CodeSearchRequest>, I>>(base?: I): CodeSearchRequest {
    return CodeSearchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeSearchRequest>, I>>(object: I): CodeSearchRequest {
    const message = createBaseCodeSearchRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.workspaceId = object.workspaceId ?? "";
    message.query = object.query ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseCodeSearchResult(): CodeSearchResult {
  return { path: "", line: 0, symbol: "", method: "" };
}

export const CodeSearchResult: MessageFns<CodeSearchResult> = {
  encode(message: CodeSearchResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.line !== 0) {
      writer.uint32(16).uint32(message.line);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.method !== "") {
      writer.uint32(34).string(message.method);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeSearchResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodeSearchResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.line = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.method = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CodeSearchResult>, I>>(base?: I): CodeSearchResult {
    return CodeSearchResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeSearchResult>, I>>(object: I): CodeSearchResult {
    const message = createBaseCodeSearchResult();
    message.path = object.path ?? "";
    message.line = object.line ?? 0;
    message.symbol = object.symbol ?? "";
    message.method = object.method ?? "";
    return message;
  },
};

function createBaseCodeSearchResponse(): CodeSearchResponse {
  return { results: [], truncated: false, continuation: undefined };
}

export const CodeSearchResponse: MessageFns<CodeSearchResponse> = {
  encode(message: CodeSearchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      CodeSearchResult.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.truncated !== false) {
      writer.uint32(16).bool(message.truncated);
    }
    if (message.continuation !== undefined) {
      writer.uint32(26).string(message.continuation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeSearchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodeSearchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(CodeSearchResult.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.continuation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CodeSearchResponse>, I>>(base?: I): CodeSearchResponse {
    return CodeSearchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeSearchResponse>, I>>(object: I): CodeSearchResponse {
    const message = createBaseCodeSearchResponse();
    message.results = object.results?.map((e) => CodeSearchResult.fromPartial(e)) || [];
    message.truncated = object.truncated ?? false;
    message.continuation = object.continuation ?? undefined;
    return message;
  },
};

function createBaseExtensionInvokeRequest(): ExtensionInvokeRequest {
  return { context: undefined, capabilityId: "", operation: "", input: new Uint8Array(0) };
}

export const ExtensionInvokeRequest: MessageFns<ExtensionInvokeRequest> = {
  encode(message: ExtensionInvokeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.capabilityId !== "") {
      writer.uint32(18).string(message.capabilityId);
    }
    if (message.operation !== "") {
      writer.uint32(26).string(message.operation);
    }
    if (message.input.length !== 0) {
      writer.uint32(34).bytes(message.input);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExtensionInvokeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtensionInvokeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilityId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.operation = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.input = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ExtensionInvokeRequest>, I>>(base?: I): ExtensionInvokeRequest {
    return ExtensionInvokeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExtensionInvokeRequest>, I>>(object: I): ExtensionInvokeRequest {
    const message = createBaseExtensionInvokeRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.capabilityId = object.capabilityId ?? "";
    message.operation = object.operation ?? "";
    message.input = object.input ?? new Uint8Array(0);
    return message;
  },
};

function createBaseExtensionInvokeResponse(): ExtensionInvokeResponse {
  return { output: new Uint8Array(0), ok: false, error: "" };
}

export const ExtensionInvokeResponse: MessageFns<ExtensionInvokeResponse> = {
  encode(message: ExtensionInvokeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.output.length !== 0) {
      writer.uint32(10).bytes(message.output);
    }
    if (message.ok !== false) {
      writer.uint32(16).bool(message.ok);
    }
    if (message.error !== "") {
      writer.uint32(26).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExtensionInvokeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtensionInvokeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.output = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ExtensionInvokeResponse>, I>>(base?: I): ExtensionInvokeResponse {
    return ExtensionInvokeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExtensionInvokeResponse>, I>>(object: I): ExtensionInvokeResponse {
    const message = createBaseExtensionInvokeResponse();
    message.output = object.output ?? new Uint8Array(0);
    message.ok = object.ok ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseIngestArtifactRequest(): IngestArtifactRequest {
  return { context: undefined, content: new Uint8Array(0), mediaType: "" };
}

export const IngestArtifactRequest: MessageFns<IngestArtifactRequest> = {
  encode(message: IngestArtifactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.content.length !== 0) {
      writer.uint32(18).bytes(message.content);
    }
    if (message.mediaType !== "") {
      writer.uint32(26).string(message.mediaType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestArtifactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestArtifactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IngestArtifactRequest>, I>>(base?: I): IngestArtifactRequest {
    return IngestArtifactRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestArtifactRequest>, I>>(object: I): IngestArtifactRequest {
    const message = createBaseIngestArtifactRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.content = object.content ?? new Uint8Array(0);
    message.mediaType = object.mediaType ?? "";
    return message;
  },
};

function createBaseIngestArtifactResponse(): IngestArtifactResponse {
  return { artifact: undefined, alreadyPresent: false };
}

export const IngestArtifactResponse: MessageFns<IngestArtifactResponse> = {
  encode(message: IngestArtifactResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.artifact !== undefined) {
      ArtifactRef.encode(message.artifact, writer.uint32(10).fork()).join();
    }
    if (message.alreadyPresent !== false) {
      writer.uint32(16).bool(message.alreadyPresent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestArtifactResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestArtifactResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.artifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.alreadyPresent = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IngestArtifactResponse>, I>>(base?: I): IngestArtifactResponse {
    return IngestArtifactResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestArtifactResponse>, I>>(object: I): IngestArtifactResponse {
    const message = createBaseIngestArtifactResponse();
    message.artifact = (object.artifact !== undefined && object.artifact !== null)
      ? ArtifactRef.fromPartial(object.artifact)
      : undefined;
    message.alreadyPresent = object.alreadyPresent ?? false;
    return message;
  },
};

function createBaseGetArtifactRequest(): GetArtifactRequest {
  return { context: undefined, sha256: "" };
}

export const GetArtifactRequest: MessageFns<GetArtifactRequest> = {
  encode(message: GetArtifactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.sha256 !== "") {
      writer.uint32(18).string(message.sha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetArtifactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetArtifactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetArtifactRequest>, I>>(base?: I): GetArtifactRequest {
    return GetArtifactRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetArtifactRequest>, I>>(object: I): GetArtifactRequest {
    const message = createBaseGetArtifactRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.sha256 = object.sha256 ?? "";
    return message;
  },
};

function createBaseGetArtifactResponse(): GetArtifactResponse {
  return { artifact: undefined, content: new Uint8Array(0) };
}

export const GetArtifactResponse: MessageFns<GetArtifactResponse> = {
  encode(message: GetArtifactResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.artifact !== undefined) {
      ArtifactRef.encode(message.artifact, writer.uint32(10).fork()).join();
    }
    if (message.content.length !== 0) {
      writer.uint32(18).bytes(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetArtifactResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetArtifactResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.artifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetArtifactResponse>, I>>(base?: I): GetArtifactResponse {
    return GetArtifactResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetArtifactResponse>, I>>(object: I): GetArtifactResponse {
    const message = createBaseGetArtifactResponse();
    message.artifact = (object.artifact !== undefined && object.artifact !== null)
      ? ArtifactRef.fromPartial(object.artifact)
      : undefined;
    message.content = object.content ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetArtifactMetadataRequest(): GetArtifactMetadataRequest {
  return { context: undefined, sha256: "" };
}

export const GetArtifactMetadataRequest: MessageFns<GetArtifactMetadataRequest> = {
  encode(message: GetArtifactMetadataRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.sha256 !== "") {
      writer.uint32(18).string(message.sha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetArtifactMetadataRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetArtifactMetadataRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetArtifactMetadataRequest>, I>>(base?: I): GetArtifactMetadataRequest {
    return GetArtifactMetadataRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetArtifactMetadataRequest>, I>>(object: I): GetArtifactMetadataRequest {
    const message = createBaseGetArtifactMetadataRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.sha256 = object.sha256 ?? "";
    return message;
  },
};

function createBaseGetArtifactMetadataResponse(): GetArtifactMetadataResponse {
  return { artifact: undefined };
}

export const GetArtifactMetadataResponse: MessageFns<GetArtifactMetadataResponse> = {
  encode(message: GetArtifactMetadataResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.artifact !== undefined) {
      ArtifactRef.encode(message.artifact, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetArtifactMetadataResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetArtifactMetadataResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.artifact = ArtifactRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetArtifactMetadataResponse>, I>>(base?: I): GetArtifactMetadataResponse {
    return GetArtifactMetadataResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetArtifactMetadataResponse>, I>>(object: I): GetArtifactMetadataResponse {
    const message = createBaseGetArtifactMetadataResponse();
    message.artifact = (object.artifact !== undefined && object.artifact !== null)
      ? ArtifactRef.fromPartial(object.artifact)
      : undefined;
    return message;
  },
};

function createBaseLinkArtifactRequest(): LinkArtifactRequest {
  return { context: undefined, sha256: "", ownerType: "", ownerId: "", purpose: "", ownerTaskId: "" };
}

export const LinkArtifactRequest: MessageFns<LinkArtifactRequest> = {
  encode(message: LinkArtifactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.sha256 !== "") {
      writer.uint32(18).string(message.sha256);
    }
    if (message.ownerType !== "") {
      writer.uint32(26).string(message.ownerType);
    }
    if (message.ownerId !== "") {
      writer.uint32(34).string(message.ownerId);
    }
    if (message.purpose !== "") {
      writer.uint32(42).string(message.purpose);
    }
    if (message.ownerTaskId !== "") {
      writer.uint32(50).string(message.ownerTaskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LinkArtifactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLinkArtifactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.purpose = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ownerTaskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<LinkArtifactRequest>, I>>(base?: I): LinkArtifactRequest {
    return LinkArtifactRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LinkArtifactRequest>, I>>(object: I): LinkArtifactRequest {
    const message = createBaseLinkArtifactRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.sha256 = object.sha256 ?? "";
    message.ownerType = object.ownerType ?? "";
    message.ownerId = object.ownerId ?? "";
    message.purpose = object.purpose ?? "";
    message.ownerTaskId = object.ownerTaskId ?? "";
    return message;
  },
};

function createBaseLinkArtifactResponse(): LinkArtifactResponse {
  return { linked: false };
}

export const LinkArtifactResponse: MessageFns<LinkArtifactResponse> = {
  encode(message: LinkArtifactResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.linked !== false) {
      writer.uint32(8).bool(message.linked);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LinkArtifactResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLinkArtifactResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.linked = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<LinkArtifactResponse>, I>>(base?: I): LinkArtifactResponse {
    return LinkArtifactResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LinkArtifactResponse>, I>>(object: I): LinkArtifactResponse {
    const message = createBaseLinkArtifactResponse();
    message.linked = object.linked ?? false;
    return message;
  },
};

function createBaseListCheckpointArtifactLinksRequest(): ListCheckpointArtifactLinksRequest {
  return { context: undefined, pageSize: 0, continuationToken: "" };
}

export const ListCheckpointArtifactLinksRequest: MessageFns<ListCheckpointArtifactLinksRequest> = {
  encode(message: ListCheckpointArtifactLinksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.pageSize !== 0) {
      writer.uint32(16).uint32(message.pageSize);
    }
    if (message.continuationToken !== "") {
      writer.uint32(26).string(message.continuationToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCheckpointArtifactLinksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCheckpointArtifactLinksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pageSize = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.continuationToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListCheckpointArtifactLinksRequest>, I>>(
    base?: I,
  ): ListCheckpointArtifactLinksRequest {
    return ListCheckpointArtifactLinksRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCheckpointArtifactLinksRequest>, I>>(
    object: I,
  ): ListCheckpointArtifactLinksRequest {
    const message = createBaseListCheckpointArtifactLinksRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.pageSize = object.pageSize ?? 0;
    message.continuationToken = object.continuationToken ?? "";
    return message;
  },
};

function createBaseCheckpointArtifactLinkMessage(): CheckpointArtifactLinkMessage {
  return { linkId: "", sha256: "", checkpointId: "", ownerTaskId: "", createdAt: "" };
}

export const CheckpointArtifactLinkMessage: MessageFns<CheckpointArtifactLinkMessage> = {
  encode(message: CheckpointArtifactLinkMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.linkId !== "") {
      writer.uint32(10).string(message.linkId);
    }
    if (message.sha256 !== "") {
      writer.uint32(18).string(message.sha256);
    }
    if (message.checkpointId !== "") {
      writer.uint32(26).string(message.checkpointId);
    }
    if (message.ownerTaskId !== "") {
      writer.uint32(34).string(message.ownerTaskId);
    }
    if (message.createdAt !== "") {
      writer.uint32(42).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckpointArtifactLinkMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckpointArtifactLinkMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.linkId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.checkpointId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ownerTaskId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CheckpointArtifactLinkMessage>, I>>(base?: I): CheckpointArtifactLinkMessage {
    return CheckpointArtifactLinkMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CheckpointArtifactLinkMessage>, I>>(
    object: I,
  ): CheckpointArtifactLinkMessage {
    const message = createBaseCheckpointArtifactLinkMessage();
    message.linkId = object.linkId ?? "";
    message.sha256 = object.sha256 ?? "";
    message.checkpointId = object.checkpointId ?? "";
    message.ownerTaskId = object.ownerTaskId ?? "";
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

function createBaseListCheckpointArtifactLinksResponse(): ListCheckpointArtifactLinksResponse {
  return { links: [], continuationToken: "" };
}

export const ListCheckpointArtifactLinksResponse: MessageFns<ListCheckpointArtifactLinksResponse> = {
  encode(message: ListCheckpointArtifactLinksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.links) {
      CheckpointArtifactLinkMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.continuationToken !== "") {
      writer.uint32(18).string(message.continuationToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCheckpointArtifactLinksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCheckpointArtifactLinksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.links.push(CheckpointArtifactLinkMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.continuationToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListCheckpointArtifactLinksResponse>, I>>(
    base?: I,
  ): ListCheckpointArtifactLinksResponse {
    return ListCheckpointArtifactLinksResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCheckpointArtifactLinksResponse>, I>>(
    object: I,
  ): ListCheckpointArtifactLinksResponse {
    const message = createBaseListCheckpointArtifactLinksResponse();
    message.links = object.links?.map((e) => CheckpointArtifactLinkMessage.fromPartial(e)) || [];
    message.continuationToken = object.continuationToken ?? "";
    return message;
  },
};

function createBaseUnlinkCheckpointArtifactRequest(): UnlinkCheckpointArtifactRequest {
  return { context: undefined, sha256: "", checkpointId: "", ownerTaskId: "" };
}

export const UnlinkCheckpointArtifactRequest: MessageFns<UnlinkCheckpointArtifactRequest> = {
  encode(message: UnlinkCheckpointArtifactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.sha256 !== "") {
      writer.uint32(18).string(message.sha256);
    }
    if (message.checkpointId !== "") {
      writer.uint32(26).string(message.checkpointId);
    }
    if (message.ownerTaskId !== "") {
      writer.uint32(34).string(message.ownerTaskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnlinkCheckpointArtifactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnlinkCheckpointArtifactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.checkpointId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ownerTaskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<UnlinkCheckpointArtifactRequest>, I>>(base?: I): UnlinkCheckpointArtifactRequest {
    return UnlinkCheckpointArtifactRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnlinkCheckpointArtifactRequest>, I>>(
    object: I,
  ): UnlinkCheckpointArtifactRequest {
    const message = createBaseUnlinkCheckpointArtifactRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.sha256 = object.sha256 ?? "";
    message.checkpointId = object.checkpointId ?? "";
    message.ownerTaskId = object.ownerTaskId ?? "";
    return message;
  },
};

function createBaseUnlinkCheckpointArtifactResponse(): UnlinkCheckpointArtifactResponse {
  return { unlinked: false };
}

export const UnlinkCheckpointArtifactResponse: MessageFns<UnlinkCheckpointArtifactResponse> = {
  encode(message: UnlinkCheckpointArtifactResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.unlinked !== false) {
      writer.uint32(8).bool(message.unlinked);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnlinkCheckpointArtifactResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnlinkCheckpointArtifactResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.unlinked = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<UnlinkCheckpointArtifactResponse>, I>>(
    base?: I,
  ): UnlinkCheckpointArtifactResponse {
    return UnlinkCheckpointArtifactResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnlinkCheckpointArtifactResponse>, I>>(
    object: I,
  ): UnlinkCheckpointArtifactResponse {
    const message = createBaseUnlinkCheckpointArtifactResponse();
    message.unlinked = object.unlinked ?? false;
    return message;
  },
};

export interface KernelInfoService {
  GetInfo(request: Empty): Promise<KernelInfo>;
  Health(request: Empty): Promise<KernelHealth>;
  /** Restricted-UDS-only standalone bootstrap. Never exposed by HTTP/TCP. */
  BootstrapControl(request: BootstrapControlRequest): Promise<BootstrapControlCapabilities>;
}

export const KernelInfoServiceServiceName = "terminus.kernel.v1.KernelInfoService";
export class KernelInfoServiceClientImpl implements KernelInfoService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || KernelInfoServiceServiceName;
    this.rpc = rpc;
    this.GetInfo = this.GetInfo.bind(this);
    this.Health = this.Health.bind(this);
    this.BootstrapControl = this.BootstrapControl.bind(this);
  }
  GetInfo(request: Empty): Promise<KernelInfo> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetInfo", data);
    return promise.then((data) => KernelInfo.decode(new BinaryReader(data)));
  }

  Health(request: Empty): Promise<KernelHealth> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "Health", data);
    return promise.then((data) => KernelHealth.decode(new BinaryReader(data)));
  }

  BootstrapControl(request: BootstrapControlRequest): Promise<BootstrapControlCapabilities> {
    const data = BootstrapControlRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "BootstrapControl", data);
    return promise.then((data) => BootstrapControlCapabilities.decode(new BinaryReader(data)));
  }
}

export interface FileService {
  Read(request: ReadFileRequest): Promise<ReadFileResponse>;
}

export const FileServiceServiceName = "terminus.kernel.v1.FileService";
export class FileServiceClientImpl implements FileService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || FileServiceServiceName;
    this.rpc = rpc;
    this.Read = this.Read.bind(this);
  }
  Read(request: ReadFileRequest): Promise<ReadFileResponse> {
    const data = ReadFileRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Read", data);
    return promise.then((data) => ReadFileResponse.decode(new BinaryReader(data)));
  }
}

export interface PatchService {
  Apply(request: PatchRequest): Promise<PatchResponse>;
  Reconcile(request: PatchReconcileRequest): Promise<PatchResponse>;
}

export const PatchServiceServiceName = "terminus.kernel.v1.PatchService";
export class PatchServiceClientImpl implements PatchService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || PatchServiceServiceName;
    this.rpc = rpc;
    this.Apply = this.Apply.bind(this);
    this.Reconcile = this.Reconcile.bind(this);
  }
  Apply(request: PatchRequest): Promise<PatchResponse> {
    const data = PatchRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Apply", data);
    return promise.then((data) => PatchResponse.decode(new BinaryReader(data)));
  }

  Reconcile(request: PatchReconcileRequest): Promise<PatchResponse> {
    const data = PatchReconcileRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Reconcile", data);
    return promise.then((data) => PatchResponse.decode(new BinaryReader(data)));
  }
}

export interface ProcessService {
  Start(request: StartProcessRequest): Observable<ProcessEvent>;
  Cancel(request: CancelProcessRequest): Promise<CancelProcessResponse>;
}

export const ProcessServiceServiceName = "terminus.kernel.v1.ProcessService";
export class ProcessServiceClientImpl implements ProcessService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ProcessServiceServiceName;
    this.rpc = rpc;
    this.Start = this.Start.bind(this);
    this.Cancel = this.Cancel.bind(this);
  }
  Start(request: StartProcessRequest): Observable<ProcessEvent> {
    const data = StartProcessRequest.encode(request).finish();
    const result = this.rpc.serverStreamingRequest(this.service, "Start", data);
    return result.pipe(map((data) => ProcessEvent.decode(new BinaryReader(data))));
  }

  Cancel(request: CancelProcessRequest): Promise<CancelProcessResponse> {
    const data = CancelProcessRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Cancel", data);
    return promise.then((data) => CancelProcessResponse.decode(new BinaryReader(data)));
  }
}

export interface JobService {
  Start(request: StartJobRequest): Promise<StartJobResponse>;
  Stream(request: JobStreamRequest): Observable<JobEvent>;
  Input(request: JobInputRequest): Promise<JobState>;
  Signal(request: JobSignalRequest): Promise<JobState>;
  Stop(request: JobStopRequest): Promise<JobState>;
  Get(request: JobGetRequest): Promise<JobState>;
}

export const JobServiceServiceName = "terminus.kernel.v1.JobService";
export class JobServiceClientImpl implements JobService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || JobServiceServiceName;
    this.rpc = rpc;
    this.Start = this.Start.bind(this);
    this.Stream = this.Stream.bind(this);
    this.Input = this.Input.bind(this);
    this.Signal = this.Signal.bind(this);
    this.Stop = this.Stop.bind(this);
    this.Get = this.Get.bind(this);
  }
  Start(request: StartJobRequest): Promise<StartJobResponse> {
    const data = StartJobRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Start", data);
    return promise.then((data) => StartJobResponse.decode(new BinaryReader(data)));
  }

  Stream(request: JobStreamRequest): Observable<JobEvent> {
    const data = JobStreamRequest.encode(request).finish();
    const result = this.rpc.serverStreamingRequest(this.service, "Stream", data);
    return result.pipe(map((data) => JobEvent.decode(new BinaryReader(data))));
  }

  Input(request: JobInputRequest): Promise<JobState> {
    const data = JobInputRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Input", data);
    return promise.then((data) => JobState.decode(new BinaryReader(data)));
  }

  Signal(request: JobSignalRequest): Promise<JobState> {
    const data = JobSignalRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Signal", data);
    return promise.then((data) => JobState.decode(new BinaryReader(data)));
  }

  Stop(request: JobStopRequest): Promise<JobState> {
    const data = JobStopRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Stop", data);
    return promise.then((data) => JobState.decode(new BinaryReader(data)));
  }

  Get(request: JobGetRequest): Promise<JobState> {
    const data = JobGetRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => JobState.decode(new BinaryReader(data)));
  }
}

export interface WorkspaceService {
  /** Read-only canonicalization used before durable identity/trust admission. */
  ResolveRoot(request: ResolveWorkspaceRootRequest): Promise<ResolvedWorkspaceRootMessage>;
  Register(request: RegisterWorkspaceRequest): Promise<WorkspaceEntryMessage>;
  Get(request: GetWorkspaceRequest): Promise<WorkspaceEntryMessage>;
}

export const WorkspaceServiceServiceName = "terminus.kernel.v1.WorkspaceService";
export class WorkspaceServiceClientImpl implements WorkspaceService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || WorkspaceServiceServiceName;
    this.rpc = rpc;
    this.ResolveRoot = this.ResolveRoot.bind(this);
    this.Register = this.Register.bind(this);
    this.Get = this.Get.bind(this);
  }
  ResolveRoot(request: ResolveWorkspaceRootRequest): Promise<ResolvedWorkspaceRootMessage> {
    const data = ResolveWorkspaceRootRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ResolveRoot", data);
    return promise.then((data) => ResolvedWorkspaceRootMessage.decode(new BinaryReader(data)));
  }

  Register(request: RegisterWorkspaceRequest): Promise<WorkspaceEntryMessage> {
    const data = RegisterWorkspaceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Register", data);
    return promise.then((data) => WorkspaceEntryMessage.decode(new BinaryReader(data)));
  }

  Get(request: GetWorkspaceRequest): Promise<WorkspaceEntryMessage> {
    const data = GetWorkspaceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => WorkspaceEntryMessage.decode(new BinaryReader(data)));
  }
}

export interface SandboxService {
  Report(request: SandboxReportRequest): Promise<EnforcementReportMessage>;
}

export const SandboxServiceServiceName = "terminus.kernel.v1.SandboxService";
export class SandboxServiceClientImpl implements SandboxService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || SandboxServiceServiceName;
    this.rpc = rpc;
    this.Report = this.Report.bind(this);
  }
  Report(request: SandboxReportRequest): Promise<EnforcementReportMessage> {
    const data = SandboxReportRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Report", data);
    return promise.then((data) => EnforcementReportMessage.decode(new BinaryReader(data)));
  }
}

export interface PolicyService {
  Evaluate(request: EvaluatePolicyRequest): Promise<DecisionReportMessage>;
  MintTaskCapability(request: MintTaskCapabilityRequest): Promise<MintTaskCapabilityResponse>;
}

export const PolicyServiceServiceName = "terminus.kernel.v1.PolicyService";
export class PolicyServiceClientImpl implements PolicyService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || PolicyServiceServiceName;
    this.rpc = rpc;
    this.Evaluate = this.Evaluate.bind(this);
    this.MintTaskCapability = this.MintTaskCapability.bind(this);
  }
  Evaluate(request: EvaluatePolicyRequest): Promise<DecisionReportMessage> {
    const data = EvaluatePolicyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Evaluate", data);
    return promise.then((data) => DecisionReportMessage.decode(new BinaryReader(data)));
  }

  MintTaskCapability(request: MintTaskCapabilityRequest): Promise<MintTaskCapabilityResponse> {
    const data = MintTaskCapabilityRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "MintTaskCapability", data);
    return promise.then((data) => MintTaskCapabilityResponse.decode(new BinaryReader(data)));
  }
}

export interface SecretService {
  Mint(request: MintSecretRequest): Promise<SecretCapabilityMessage>;
  Store(request: StoreSecretRequest): Promise<SecretMutationResponse>;
  Delete(request: DeleteSecretRequest): Promise<SecretMutationResponse>;
}

export const SecretServiceServiceName = "terminus.kernel.v1.SecretService";
export class SecretServiceClientImpl implements SecretService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || SecretServiceServiceName;
    this.rpc = rpc;
    this.Mint = this.Mint.bind(this);
    this.Store = this.Store.bind(this);
    this.Delete = this.Delete.bind(this);
  }
  Mint(request: MintSecretRequest): Promise<SecretCapabilityMessage> {
    const data = MintSecretRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Mint", data);
    return promise.then((data) => SecretCapabilityMessage.decode(new BinaryReader(data)));
  }

  Store(request: StoreSecretRequest): Promise<SecretMutationResponse> {
    const data = StoreSecretRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Store", data);
    return promise.then((data) => SecretMutationResponse.decode(new BinaryReader(data)));
  }

  Delete(request: DeleteSecretRequest): Promise<SecretMutationResponse> {
    const data = DeleteSecretRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Delete", data);
    return promise.then((data) => SecretMutationResponse.decode(new BinaryReader(data)));
  }
}

export interface NetworkService {
  Decide(request: EgressRequest): Promise<EgressDecisionMessage>;
}

export const NetworkServiceServiceName = "terminus.kernel.v1.NetworkService";
export class NetworkServiceClientImpl implements NetworkService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || NetworkServiceServiceName;
    this.rpc = rpc;
    this.Decide = this.Decide.bind(this);
  }
  Decide(request: EgressRequest): Promise<EgressDecisionMessage> {
    const data = EgressRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Decide", data);
    return promise.then((data) => EgressDecisionMessage.decode(new BinaryReader(data)));
  }
}

export interface ConnectorService {
  MintGrant(request: MintConnectorGrantRequest): Promise<ConnectorGrantMessage>;
  Execute(request: ExecuteConnectorRequest): Promise<ConnectorResponseMessage>;
}

export const ConnectorServiceServiceName = "terminus.kernel.v1.ConnectorService";
export class ConnectorServiceClientImpl implements ConnectorService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ConnectorServiceServiceName;
    this.rpc = rpc;
    this.MintGrant = this.MintGrant.bind(this);
    this.Execute = this.Execute.bind(this);
  }
  MintGrant(request: MintConnectorGrantRequest): Promise<ConnectorGrantMessage> {
    const data = MintConnectorGrantRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "MintGrant", data);
    return promise.then((data) => ConnectorGrantMessage.decode(new BinaryReader(data)));
  }

  Execute(request: ExecuteConnectorRequest): Promise<ConnectorResponseMessage> {
    const data = ExecuteConnectorRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Execute", data);
    return promise.then((data) => ConnectorResponseMessage.decode(new BinaryReader(data)));
  }
}

export interface CodeIntelligenceService {
  Search(request: CodeSearchRequest): Promise<CodeSearchResponse>;
}

export const CodeIntelligenceServiceServiceName = "terminus.kernel.v1.CodeIntelligenceService";
export class CodeIntelligenceServiceClientImpl implements CodeIntelligenceService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || CodeIntelligenceServiceServiceName;
    this.rpc = rpc;
    this.Search = this.Search.bind(this);
  }
  Search(request: CodeSearchRequest): Promise<CodeSearchResponse> {
    const data = CodeSearchRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Search", data);
    return promise.then((data) => CodeSearchResponse.decode(new BinaryReader(data)));
  }
}

export interface ExtensionRuntimeService {
  Invoke(request: ExtensionInvokeRequest): Promise<ExtensionInvokeResponse>;
}

export const ExtensionRuntimeServiceServiceName = "terminus.kernel.v1.ExtensionRuntimeService";
export class ExtensionRuntimeServiceClientImpl implements ExtensionRuntimeService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ExtensionRuntimeServiceServiceName;
    this.rpc = rpc;
    this.Invoke = this.Invoke.bind(this);
  }
  Invoke(request: ExtensionInvokeRequest): Promise<ExtensionInvokeResponse> {
    const data = ExtensionInvokeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Invoke", data);
    return promise.then((data) => ExtensionInvokeResponse.decode(new BinaryReader(data)));
  }
}

export interface ArtifactIngestService {
  Ingest(request: IngestArtifactRequest): Promise<IngestArtifactResponse>;
  Get(request: GetArtifactRequest): Promise<GetArtifactResponse>;
  GetMetadata(request: GetArtifactMetadataRequest): Promise<GetArtifactMetadataResponse>;
  Link(request: LinkArtifactRequest): Promise<LinkArtifactResponse>;
  ListCheckpointLinks(request: ListCheckpointArtifactLinksRequest): Promise<ListCheckpointArtifactLinksResponse>;
  UnlinkCheckpoint(request: UnlinkCheckpointArtifactRequest): Promise<UnlinkCheckpointArtifactResponse>;
}

export const ArtifactIngestServiceServiceName = "terminus.kernel.v1.ArtifactIngestService";
export class ArtifactIngestServiceClientImpl implements ArtifactIngestService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ArtifactIngestServiceServiceName;
    this.rpc = rpc;
    this.Ingest = this.Ingest.bind(this);
    this.Get = this.Get.bind(this);
    this.GetMetadata = this.GetMetadata.bind(this);
    this.Link = this.Link.bind(this);
    this.ListCheckpointLinks = this.ListCheckpointLinks.bind(this);
    this.UnlinkCheckpoint = this.UnlinkCheckpoint.bind(this);
  }
  Ingest(request: IngestArtifactRequest): Promise<IngestArtifactResponse> {
    const data = IngestArtifactRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Ingest", data);
    return promise.then((data) => IngestArtifactResponse.decode(new BinaryReader(data)));
  }

  Get(request: GetArtifactRequest): Promise<GetArtifactResponse> {
    const data = GetArtifactRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => GetArtifactResponse.decode(new BinaryReader(data)));
  }

  GetMetadata(request: GetArtifactMetadataRequest): Promise<GetArtifactMetadataResponse> {
    const data = GetArtifactMetadataRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetMetadata", data);
    return promise.then((data) => GetArtifactMetadataResponse.decode(new BinaryReader(data)));
  }

  Link(request: LinkArtifactRequest): Promise<LinkArtifactResponse> {
    const data = LinkArtifactRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Link", data);
    return promise.then((data) => LinkArtifactResponse.decode(new BinaryReader(data)));
  }

  ListCheckpointLinks(request: ListCheckpointArtifactLinksRequest): Promise<ListCheckpointArtifactLinksResponse> {
    const data = ListCheckpointArtifactLinksRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListCheckpointLinks", data);
    return promise.then((data) => ListCheckpointArtifactLinksResponse.decode(new BinaryReader(data)));
  }

  UnlinkCheckpoint(request: UnlinkCheckpointArtifactRequest): Promise<UnlinkCheckpointArtifactResponse> {
    const data = UnlinkCheckpointArtifactRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UnlinkCheckpoint", data);
    return promise.then((data) => UnlinkCheckpointArtifactResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
