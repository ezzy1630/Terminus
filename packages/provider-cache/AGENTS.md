# @terminus/provider-cache

Cache observation recording per SPEC §38.9.

Tracks cache read/write/hit/miss events, stable-prefix drift detection,
and cache hit-rate computation. Pure functions — no network I/O.
