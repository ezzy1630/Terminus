suite: build-failure
task: build-001
cohort: build_failure
title: Fix missing import causing compile error
source_commit: fixture-build-001-v3
image_digest: local:python-3.12-pytest-8
timeout: 600
budget:
  max_cost_usd: 1.0
  max_wall_seconds: 600
  max_tool_calls: 50
  max_turns: 20
allowed_network: []
secrets: {}
grader_version: 1.0.0
rationale: behavioral import and regression checks for a deterministic local fixture
acceptance_criteria:
  - id: import-clean
    statement: src.main imports without an exception
    verification_hint: "command: just verify-import"
  - id: regression-tests
    statement: the existing command behavior tests pass
    verification_hint: "command: just test"
grader:
  entrypoint: grader/run.py
  protocol: json_stdio
  timeout_seconds: 30
risk_class: normal
