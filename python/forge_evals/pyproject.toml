[project]
name = "forge-evals"
version = "0.1.0"
description = "Forge evaluation laboratory — offline/non-privileged research plane (SPEC §18, §41, §43.3)."
requires-python = ">=3.12"
authors = [{ name = "Forge" }]
license = "Apache-2.0"
dependencies = [
    "pydantic>=2.0",
    "click>=8.0",
    "rich>=13.0",
]

[project.optional-dependencies]
analysis = [
    "polars>=1.0",
    "duckdb>=1.0",
    "pyarrow>=15.0",
]

[project.scripts]
forge-eval = "forge_evals.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["forge_evals"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
