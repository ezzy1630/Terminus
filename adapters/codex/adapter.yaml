adapter:
  id: codex
  version: 0.1.0
  status: stub
  inner_harness_version: pinned
  description: |
    OpenAI Codex adapter. Codex is a strong implementer with native
    session resume and typed tool events, but its context and sandbox
    semantics are partly opaque. Terminus supplies an outer sandbox and
    brokered network; the adapter's claimed capabilities are
    cross-checked by live probes.
  capabilities:
    exact_context_visibility: partial
    tool_interception: partial
    filesystem_enforcement: outer_sandbox
    network_enforcement: outer_sandbox
    secret_isolation: outer_broker
    session_resume: native
    typed_results: native
    artifact_export: partial
    cancellation: reliable
    model_selection: constrained
    native_compaction: true
  # Roadmap Phase 0 truth fix: no live capability probe has ever been run
  # against this adapter. The previously recorded "probe" block and its
  # discrepancy list were unreproducible placeholders and have been removed.
  # Capabilities above are declared design intent only; observed_by_probe is
  # filled exclusively by a real terminus-probe conformance run.
  observed_by_probe: null
  discrepancies: []
  last_verified: null
  result_schema: terminus.worker.v1
  notes: |
    The adapter is enabled only when Terminus can supply the missing
    filesystem, network, and secret controls. The UI surfaces the
    discrepancies and the effective enforcement profile.
