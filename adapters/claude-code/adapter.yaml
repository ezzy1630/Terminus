adapter:
  id: claude-code
  version: 0.1.0
  inner_harness_version: floating
  description: |
    Anthropic Claude Code adapter. Claude Code is a strong implementer
    with native tool-use and structured output. Its session resume is
    emulated; its context visibility is partial. Terminus supplies an outer
    sandbox, brokered network, and brokered secrets.
  capabilities:
    exact_context_visibility: partial
    tool_interception: partial
    filesystem_enforcement: outer_sandbox
    network_enforcement: outer_sandbox
    secret_isolation: outer_broker
    session_resume: emulated
    typed_results: native
    artifact_export: complete
    cancellation: best_effort
    model_selection: constrained
    native_compaction: true
  observed_by_probe:
    probe_version: terminus-probe-1.0
    probed_at: "2025-01-15T00:00:00Z"
    probed_capabilities:
      exact_context_visibility: partial
      tool_interception: partial
      filesystem_enforcement: none
      network_enforcement: none
      secret_isolation: none
      session_resume: emulated
      typed_results: native
      artifact_export: complete
      cancellation: best_effort
      model_selection: opaque
      native_compaction: true
  discrepancies:
    - "filesystem_enforcement: declared outer_sandbox, observed none — Terminus MUST supply outer sandbox"
    - "network_enforcement: declared outer_sandbox, observed none — Terminus MUST supply outer sandbox"
    - "secret_isolation: declared outer_broker, observed none — Terminus MUST broker secrets"
    - "model_selection: declared constrained, observed opaque — Terminus MUST control model selection"
    - "cancellation: declared best_effort, observed best_effort — verify timeout behavior"
  last_verified: "2025-01-15T00:00:00Z"
  result_schema: terminus.worker.v1
  notes: |
    The adapter is enabled for tasks where Claude Code's strengths
    (strong implementer, structured output) outweigh the cost of
    supplying outer controls. Automation and licensing constraints may
    apply; see SPEC §41.2.
